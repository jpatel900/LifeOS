.cursor
.turbo
.next
dist
node_modules
coverage
supabase/**/*.sql
supabase/config.toml
pnpm-lock.yaml
# Vendored/exported design-tool output and a scratch prototype workshop page —
# not hand-authored source; reformatting would rewrite generated structure
# wholesale rather than fix real drift. See PR for chore/ci-cost-hardening.
design_handoff_lifeos/**
docs/implementation-planning/ui-handoff-conflicts/**
# Vendored skill-hub content synced verbatim by the daily sync workflow —
# externally authored, and some files are prettier-non-idempotent (format
# then check still fails), which redded main on 2026-07-10 (#496 -> revert
# #499). See issue #508.
.agents/skills/**
