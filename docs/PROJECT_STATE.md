# PROJECT_STATE.md

## Current status

P0 UX trust-repair closure is complete. Final Playwright MCP regression evidence passed 14/14 critical journeys, and a durable browser-level regression suite now exists for those P0 paths. Today/Home Cockpit v1 is now implemented on `/`: Home is no longer a static placeholder and now acts as a deterministic instrument panel over existing workflow state with one dominant Next action, honest empty/degraded states, sectioned links to existing screens, and no hidden planning/execution/calendar/health writes. Issue #41's mobile follow-up is now landed: at narrow widths, Home shortens dense copy, reduces competing empty-state CTAs, reorders secondary cards by deterministic next-action priority, and keeps the route read-only. The next screen-specific workflow pass is also landed: Home now hides empty/system-secondary cards unless they are relevant, Capture now uses clearer save/sort outcome language plus human lifecycle labels for recent captures, and Triage now behaves like a current-item queue with explicit up-next switching instead of rendering every draft as an equally expanded card. The next safe continuity batch is now also landed: Capture, Triage, Planning, Execute, and Review all use the same lifecycle chip vocabulary for captured/needs-decision/accepted/planned/in-focus/completed/reviewed states, and Capture now exposes a visible `Ctrl/Cmd + Enter` save affordance from the main textarea without introducing a broader command system. Planning is now also clarified end-to-end: `/calendar` now leads with `Planning flow`, uses `Needs a suggested time`, `Ready to review`, and `Already planned` groupings, keeps Google Calendar writes secondary and explicitly approval-gated, and removes raw proposal-status leakage from the primary surface. Capture now frames raw thought first with a stronger area landing panel and a clearer local-only draft handoff, while Triage now uses `Current focus`, `Review current item`, and `Waiting after this` to keep one current decision visually primary. Review, Health, and Areas now use the same calmer hierarchy language: Review is closure-first, Health leads with the top reliability answer and `No active warnings` when appropriate, and Areas is a quieter admin surface without losing save-mode or area-diagnostics truth. Capture history truth is now also explicit: saved captures and device-only organized captures render in separate sections, saved captures appear in the right surface immediately after save, and seeded/custom area filtering now normalizes workflow ids against persisted area ids instead of comparing them naively.

MVP supports task capture, area assignment, optional AI/mock parse capture, manual scheduling, execution tracking, basic review logging, and deterministic health checks. The Phase 2 mock vertical slice remains for fallback flows. Phase 9B now clarifies source-of-truth messaging across AppShell, Capture, Triage, and Settings so users can distinguish local session workflow state from persisted provider-backed rows without changing persistence behavior. Phase 9C now hardens execution truthfulness and review source correctness: persisted `/execute` terminal updates no longer rely on hardcoded completion values and now require explicit end-session outcome/duration/productivity/notes input, and persisted `/review` summaries no longer read local session capture/review-log counts. Phase 9D now hardens planning/Google approval failure clarity: `/calendar` explicitly separates local proposal state, conflict-check state, approval gating, and Google write state; failure copy is normalized for disconnected/unconfigured/unauthenticated/duplicate/write-failed cases; and server routes now return sanitized failure messages instead of raw internal error text. Phase 9E now hardens health truthfulness and regression depth: `/health` treats optional-disabled/local-normal states (mock-only mode, signed-out persisted checks, optional AI/Google integration disabled/disconnected) as informational non-blocking checks instead of warning-like failures, while real auth/data-read failures remain explicit and actionable. P0 PR-B now hardens Health readiness: `/health` run/load now times out into a plain-language actionable error instead of hanging indefinitely, the Run button shows visible disabled reason text while checks are in progress, and completion/failure feedback is shown near the control after each run. Phase 5 parse capture is wired through a server route: `/capture` can persist raw `capture_items` first, then call the server-only Responses API parser when `OPENAI_API_KEY` and `AI_MODEL_STANDARD` are configured and `AI_PARSE_CAPTURE_ENABLED` is not disabled, or fall back to the mock parser when no API key is present or AI parsing is disabled. Parsed task/project drafts are local triage staging state for now, not durable draft rows; durability begins when triage accepts them into `tasks`/`projects`. Phase 4E persistence remains in place: `/settings/areas` reads Supabase-backed areas, `/triage` persists accepted task/project drafts to `tasks`/`projects`, `/calendar` can create/edit/reject/accept local `time_block_proposals` from persisted tasks, `/execute` persists `execution_sessions`, `/review` can create `review_entries` from persisted tasks/blocks/sessions, and `/health` shows deterministic subsystem checks from mock/Supabase/auth/app-state signals. Phase 7C adds Google Calendar connect/callback/disconnect routes plus a settings UI surface for connection status. The token-storage prerequisite persists encrypted Google OAuth tokens server-side in `google_calendar_connections`, clears them on disconnect, and keeps token material server-only. Phase 7D adds optional manual Google Calendar free/busy conflict checks for persisted local time-block proposals. Phase 7E now adds explicit approval-gated Google Calendar event creation from an existing persisted local proposal: `/calendar` can show the first-write warning, the user must click `Create Google Calendar event`, `/api/google-calendar/create-event` inserts the event server-side, stores `google_event_id` on a local `calendar_blocks` row only after Google returns an event id, marks the proposal accepted, and logs success or failure in `external_write_events`. Phase 7F hardens the approval flow by moving encrypted Google token reads/writes and audit writes behind a server-only Supabase service-role client, limiting authenticated browser Data API access to safe connection metadata plus read-only audit visibility, and using deterministic Google event IDs derived from proposal UUIDs for retry-safe inserts. No event update/delete, background mutation, full sync, autonomous scheduling, conflict solver, or AI-triggered write path exists. Phase 8B adds the shared observability safety layer, Phase 8C enables Sentry only through that wrapper, Phase 8D adds PostHog as an optional client-only manual analytics adapter through the same wrapper, Phase 8E adds optional server-only Langfuse tracing for `parse_capture` only, and Phase 8F now hardens the rollout surface with a dedicated observability runbook, provider-state verification coverage, stricter direct-import guards, sanitized request-error capture through the shared wrapper, and explicit smoke-proof guidance. Langfuse remains a no-op unless `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, and `LANGFUSE_BASE_URL` are all present; traces use manual metadata-only generations and must not export raw captures, prompts, completions, parsed object bodies, titles, calendar data, or secrets.
Phase 9F (PR1) now hardens action truthfulness and button semantics without behavior changes: AppShell quick capture is explicitly labeled as browser-local, Triage defer/reassign actions are renamed and disclosed as note-only browser edits, and Execute stop semantics are clarified as demo-mode local-only with explicit persisted-mode disable rationale. PR1 follow-up now also removes canned AppShell quick-note text generation (user-entered quick note only), makes Capture save outcomes explicit with next-step guidance, renames browser-local organize actions, replaces fake Triage edit suffix behavior with real inline draft editing, and adds post-accept next-step CTAs without changing schema/persistence/auth/parser/calendar boundaries.
P1 hardening now clarifies transient non-blocking load states on Capture/Triage/Calendar/Execute/Review, makes the theme toggle hydration-disabled state explicit with nearby reason text, and resolves `/favicon.ico` by routing it to the shipped app icon to remove repeated 404 favicon console noise.
UI modernization now delivers a dark-mode-first shell and screen system using local shadcn-style components plus theme tokens in `globals.css` and `next-themes`-driven theme state, while preserving existing workflow behavior, local/mock fallback behavior, and all external-write safety boundaries.
E2E command cleanup is now more robust in this Windows workspace: `pnpm --filter @lifeos/web test:e2e` uses a small repo script that starts the same Next dev server path explicitly, waits for readiness, prewarms the core workflow/API routes so Playwright does not start on cold route compiles, runs Playwright with the config’s built-in `webServer` disabled for that invocation only, and then tears the dev server down itself. Direct `playwright test` still keeps the config-managed `webServer` path for ad hoc use.
The Playwright wrapper is now also safer under local port contention and cold-route failures: it uses an isolated ephemeral port by default, still honors `PLAYWRIGHT_PORT` when explicitly set, fails visibly if the spawned Next dev server exits before readiness, and avoids the shell-wrapped transient `404` / `net::ERR_ABORTED` route failures that were showing up on first cold navigations in this OneDrive-backed workspace.
PR2 now extends dark-mode token compliance to Calendar, Execute, Review, and the Settings Google Calendar panel by replacing light-only inline status colors with semantic tokenized UI primitives while keeping behavior and workflow logic unchanged.
PR2 planning-controls follow-up now makes `/calendar` actions feel real without weakening approval gates: quick proposal defaults are explicitly labeled as adjustable, proposal cards are grouped into local/Google/admin action sections, inline time adjustments replace toy-feeling `Edit +30 min`, disabled Google actions now show direct per-button reasons, and Google event creation remains explicit-approval only with no automatic external writes.
PR3 now strengthens primary-action clarity and empty-state guidance across Home, Capture, Triage, Calendar, Execute, Review, Health, and Areas: each screen now exposes one clear next action, empty states point to concrete recovery/start steps, and copy stays plain-language while preserving approval-gated calendar writes and existing persistence/auth/parser behavior.
PR4 now removes internal implementation jargon from primary workflow copy across Capture, Triage, Planning, Execute, Review, Health, and Areas: user-facing surfaces now use plain language (for example Demo mode, Saved workspace, AI sorting, Planned time blocks), while implementation identifiers remain discoverable behind System details / Developer details disclosures and degraded-state guidance remains explicit.
PR4 polish now also improves system feel on remaining admin/review surfaces without behavior changes: Settings Google Calendar leads with user-value and safety copy while OAuth scopes remain visible under Advanced details, Local reset now requires explicit confirmation and shows a clear success state, Health now prioritizes actionable attention items before score-heavy diagnostics while keeping connection/system details available, and Review now frames decisions with reflection prompts while preserving numeric summaries and saved entry behavior.
The current UX trust-language follow-up now replaces the remaining `Saved workspace` / `Demo mode` / `browser only` / `session workflow area` leakage with a shared save/sync vocabulary (`Saved to account`, `Saved on this device only`), unifies collapsed diagnostics under one pattern, and reworks `/health` so the top answer is `Can I rely on LifeOS today?` before raw subsystem diagnostics. No persistence, auth, parser, observability privacy, or Google Calendar write behavior changed.
PR3 Execute session-state rework now makes `/execute` a guided focus surface instead of a timer-like panel: the page now emphasizes one current mission, groups controls into start/pause/resume, end outcomes, and secondary actions, shows explicit next-step and disabled-reason guidance, and keeps timing truth explicit by avoiding fake live elapsed timers in persisted mode.
P0 Execute terminal-state coherence is now fixed: `/execute` uses explicit UI states (`not started`, `running`, `paused`, `stopped`, `completed`, `stuck`, `distracted`, `missed`) so terminal states no longer leave contradictory active-session copy or disabled controls; stop/missed/stuck/distracted now show recovery-first next actions with clear restart behavior.
The next UX continuity batch now closes the remaining safe screen-specific trust issues around Execute, Review, and Areas: `/execute` now shows only state-valid controls plus truthful device-only timing language, `/review` now leads with grouped close-the-loop summaries and keeps raw/saved details secondary, and `/settings/areas` now hides slugs from normal cards while exposing truthful counts and route actions for each area. No schema, migration, RLS, auth, parser, observability, or Google Calendar write behavior changed.
The final UX feedback batch is now landed: Capture/Triage/Planning/Execute/Review action confirmations now explain what happened and what the next useful place/state is, using existing inline alert/status patterns instead of inventing a toast system. `/triage` now routes accepted items into Planning with explicit next-step language, `/calendar` now distinguishes suggested-time, planned-block, Google-check, and Google-write outcomes with route-appropriate follow-up guidance, `/execute` now reports start/pause/terminal outcomes with direct recovery or handoff actions, and `/review` now confirms daily-review saves with close-the-loop guidance. Mock/local Planning also now emits the same feedback when a suggested time is actually planned or removed, so the route is no longer silent on those device-only actions.
Pass 5C of the active UX roadmap is now landed as well: `/health` replaces its flat run-status line with the same authored feedback family used elsewhere, and `/settings/areas` now pushes create/remove/accent/reset feedback back toward the relevant control instead of scattering it across muted inline text and footer alerts. Pass 6A is now landed too: `/health` reads more clearly as a trust-and-repair desk, and `/settings/areas` reads more clearly as a quiet ownership registry, without changing truth boundaries, persistence behavior, or save-local honesty. Pass 6B is now landed as well: `/execute` reads more clearly as a mission room, and `/review` reads more clearly as a carry-forward desk, without changing truth boundaries, persistence behavior, or save-local honesty. Home flagship/support taxonomy maintenance is now landed too: `/` keeps the same deterministic read-only cockpit logic, but the `Today next` flagship and support-card tiers are now more explicit so the route reads intentionally authored instead of merely hiding secondary cards. No schema, auth, parser, persistence, or Google write behavior changed.
The remaining shared workflow-polish batch is now also landed: Capture, Triage, Planning, Execute, Review, and Areas now use calmer shared loading states, empty states point to the next useful route instead of dead ends, Triage and Planning cards foreground area/lifecycle before secondary metadata, Review can save reflection notes alongside daily summaries, and browser-local note actions are now explicitly disclosed as note-only. No schema, persistence boundary, auth, parser, or Google Calendar write behavior changed.
The next shared feel pass is now also landed without changing semantics: the shell uses a calmer atmospheric frame, nav/current-area/time/quick-note surfaces feel more product-grade, `WorkflowPageHeader` now supports stronger editorial composition (`eyebrow`, `title`, `description`, `actions`, `spotlight`), and Review/Health/Areas now sit on that shared header system while preserving the same route contracts and truthfulness boundaries. The next Home/Execute declutter pass is now also landed: `/` defaults to one secondary support surface plus progressive disclosure for the rest, keeps Quick Capture pinned while the user is actively using it, and `/execute` now hides mission metadata, execution-truth detail, and recent-session detail behind calmer disclosures so the current mission stays visually dominant without weakening persisted-mode truthfulness. The next Triage/Planning declutter pass is now also landed: `/triage` keeps the current item primary while moving AI context, draft notes, and browser-note actions behind disclosures, and `/calendar` now shows only the next task, the next suggested block, and a short slice of planned blocks by default while moving overflow items behind calmer disclosures. Google approval gates remain explicit and unchanged. The next Capture/Planning premium-feel pass is now also landed: `/capture` now opens with a stronger raw-first editorial header, a clearer action tray, and micro-closure save/organize confirmations that feel like product feedback instead of system output, while `/calendar` now reads like one guided planning flow with visible next-move framing, calmer planning metrics, and the same explicit local-first / Google-approval truth boundaries. Triage now matches that stronger product framing as well: `/triage` opens with a real editorial header and queue metrics, keeps one visible next move, and makes acceptance feel more like a clean handoff into Planning than a CRUD-style save confirmation. Home and Execute now also follow a stricter IA rule: support surfaces preview first and disclose the rest, so these routes no longer dump full lists, full route choices, and full execution guidance at once.
Frontend governance is now explicit as well: future UI work should keep pushing shared primitives and repeated interaction patterns toward app-local shadcn-compatible components, while preserving LifeOS shell identity, route authorship, area accents, and flagship/support/admin composition as intentionally custom layers rather than flattening the product into stock shadcn screens.
The current route-level implementation plan for that rule is now complete: the app now has shared app-local `Label` and `Skeleton` primitives, `WorkflowLoadingState` uses the shared skeleton system, `DiagnosticsDisclosure` is the standard system-details wrapper instead of repeated hand-written `details/summary` blocks, `/login` uses the same authored card/form system instead of raw inline-styled markup, and the repeated primitive/disclosure seams across Home, Capture, Triage, Planning, Execute, Review, Health, Areas, and Google Calendar settings now route through the shared layer without flattening route-specific composition.

## Recently completed

- Completed the final Home-only authored-surface maintenance slice under the shipped UX system. `/` now gives `Today next` an explicit Home flagship treatment, keeps the most relevant support card intentionally featured, and pushes overflow support cards into a quieter disclosure tier without changing deterministic Next-action logic, read-only boundaries, or shell rules. Added `docs/implementation-notes/2026-06-06-home-cockpit-flagship-taxonomy-maintenance.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/page.test.tsx`, `pnpm --filter @lifeos/web test -- src/__tests__/workflowAreaAccent.test.tsx`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, and `pnpm --filter @lifeos/web test`.
- Completed Pass 6B of the active UX roadmap: stronger route identity on Execute and Review. Execute now opens with a mission-room brief, stronger mission-card framing, and more route-specific mission-state / lane-protection language while keeping the same quiet shell and authored feedback system from Pass 4A and Pass 5B. Review now opens with a stronger carry-forward header and closure flagship, adds carry-forward metrics to the top decision surface, and sharpens the framing of reflections, next actions, board view, and history without changing closure behavior or save feedback. Added `docs/implementation-notes/2026-06-06-pass-6b-execute-review-route-identity.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/executeFocusPolish.test.tsx`, `pnpm --filter @lifeos/web test -- src/__tests__/workflowAreaAccent.test.tsx`, `pnpm --filter @lifeos/web test -- src/__tests__/phase4aPersistence.test.tsx`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-hierarchy.spec.ts`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-card-accent.spec.ts tests/e2e/execute-focus-flagship.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`.
- Completed Pass 6A of the active UX roadmap: stronger route identity on Health and Areas. Health now uses a more distinct trust-and-repair header, trust-map support surface, and repair-queue framing while keeping the same authored run-feedback system from Pass 5C. Areas now uses a more distinct ownership-boundary header, current-area ownership summary, calmer single-column area records, and stronger ownership/admin framing while keeping save-local truth and action-near feedback intact. Added `docs/implementation-notes/2026-06-06-pass-6a-health-areas-route-identity.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/healthPage.test.tsx`, `pnpm --filter @lifeos/web test -- src/__tests__/workflowAreaAccent.test.tsx`, `pnpm --filter @lifeos/web test -- src/__tests__/phase4aPersistence.test.tsx`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-hierarchy.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-card-accent.spec.ts tests/e2e/execute-focus-flagship.spec.ts`.
- Completed Pass 5C of the active UX roadmap: tighter interaction cadence on Health and Areas. Health now moves run success/failure/progress feedback into the same authored action-near family used elsewhere, and Areas now does the same for create/remove/accent/reset flows so outcomes no longer feel split between muted inline text and route-footer alerts. Added `docs/implementation-notes/2026-06-06-pass-5c-health-areas-interaction-cadence.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/healthPage.test.tsx src/__tests__/phase4aPersistence.test.tsx`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`.
- Completed Pass 5B of the active UX roadmap: tighter interaction cadence on Execute and Review. Execute now upgrades its pending and failure states into the same authored feedback family as its success states while keeping the current mission unchanged unless a save actually succeeds. Review now upgrades `Creating daily review...` into an authored in-flight surface, promotes the save confirmation into the same celebration family used elsewhere, and keeps next-step links near the confirmation so closure and follow-up feel like one system instead of an older exception. Added `docs/implementation-notes/2026-06-06-pass-5b-execute-review-interaction-cadence.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/phase4aPersistence.test.tsx src/__tests__/executeFocusPolish.test.tsx`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/interaction-feedback.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`.
- Completed Pass 5A of the active UX roadmap: tighter interaction cadence on Capture and Planning. Capture now resolves save, organize, parse-success, and parse-failure outcomes to one dominant authored feedback surface at a time instead of stacking raw-save and parse-success messaging, and safe parse failures now keep the raw-capture-stored truth visible inside that same surface. Planning now upgrades in-flight actions from a plain muted status line to authored feedback and exposes `Review next suggested time block` immediately after suggestion-oriented updates so the next useful move is visible near the confirmation. Added `docs/implementation-notes/2026-06-06-pass-5a-capture-planning-interaction-cadence.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/capture.test.tsx src/__tests__/phase4aPersistence.test.tsx`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/interaction-feedback.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`.
- Completed Pass 4B of the active UX roadmap: quieter shell framing on Capture and Planning. `AppShell` now suppresses the extra shell-context band on `/capture` and `/calendar`, and the header summary spotlights on both routes are demoted so `Write it down` and `Planning flow` win the first scan faster without changing route truthfulness, keyboard reachability, parser/save semantics, persistence, or Google approval behavior. Added `docs/implementation-notes/2026-06-06-pass-4b-capture-planning-shell-quieting.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/routeSmoke.test.tsx`, `pnpm --filter @lifeos/web test -- src/__tests__/capture.test.tsx src/__tests__/workflowAreaAccent.test.tsx`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-hierarchy.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`.
- Completed Pass 4A of the active UX roadmap: quieter shell framing on Execute and Review. `AppShell` now suppresses the extra shell-context band on `/execute` and `/review`, applies a lighter route-specific shell shadow treatment there, and Execute/Review demote their main support cards so the mission/closure flagship surface wins the first scan faster without changing route truthfulness, keyboard reachability, persistence, or external-write behavior. Added `docs/implementation-notes/2026-06-06-pass-4a-execute-review-shell-quieting.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/routeSmoke.test.tsx src/__tests__/executeFocusPolish.test.tsx src/__tests__/workflowAreaAccent.test.tsx`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`.
- Completed the first runtime slice of roadmap Pass 3B and effectively closed the current explanation-by-default program. `/review` and `/settings/areas` now carry tighter support/admin copy budgets without changing any persistence, auth, parser, or external-write truth boundary: Review shortens the editorial/header copy, compresses reflection and close-the-loop helper language, tightens loading and summary wording, and trims lower-page history/admin descriptions; Areas shortens the editorial/header copy, compresses save-mode and active-area helper text, tightens create-area guidance, shortens loading and empty-state wording, and trims area-card/admin accent explanations while keeping reset/remove honesty intact. Added `docs/implementation-notes/2026-06-06-pass-3b-review-areas-explanation-reduction.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/phase4aPersistence.test.tsx src/__tests__/routeSmoke.test.tsx src/__tests__/sourceOfTruth.test.ts`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web build`.
- Completed the third runtime slice of roadmap Pass 3A. `/triage` now removes another chunk of explanation-by-default without changing queue semantics, save boundaries, or browser-note honesty: the route description is shorter, queue/save metric language is tighter, current-focus and current-item helper copy is compressed, task/project decision copy is shorter, browser-note helper text is shorter, and the empty-state and saved-context wording are more compact while preserving the same current-item hierarchy and shared primitive/disclosure layer. Added `docs/implementation-notes/2026-06-06-pass-3a-triage-explanation-reduction.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/triage.test.tsx src/__tests__/routeSmoke.test.tsx src/__tests__/sourceOfTruth.test.ts`, `pnpm --filter @lifeos/web test -- src/__tests__/phase4aPersistence.test.tsx`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web build`.
- Completed the second runtime slice of roadmap Pass 3A. `/calendar` now removes a chunk of explanation-by-default without changing any local-first or approval-gated calendar truth: the route description is shorter, the flagship planning-flow copy is compressed, support-card and empty-state descriptions are tighter, the adjust-time helper copy is shorter, and the footer safety/next-step language is more compact while preserving the same proposal and Google-write behavior. Added `docs/implementation-notes/2026-06-06-pass-3a-planning-explanation-reduction.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/phase4aPersistence.test.tsx src/__tests__/sourceOfTruth.test.ts`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, and `pnpm --filter @lifeos/web test`.
- Completed the first runtime slice of roadmap Pass 3A. `/capture` now removes a chunk of explanation-by-default without changing any save/parser/local truth boundary: the route description is shorter, the save/area/action helper copy is compressed, `On-device draft pass` is now `Local draft pass`, redundant history intro copy is gone, and the route still uses the existing shared primitive/disclosure layer rather than inventing new local seams. Added `docs/implementation-notes/2026-06-06-pass-3a-capture-explanation-reduction.md`. Validation passed with `pnpm --filter @lifeos/web test -- src/__tests__/capture.test.tsx src/__tests__/sourceOfTruth.test.ts`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-card-accent.spec.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check` (after one rerun for the known transient `.next/types` race), `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`.
- Aligned the active and historical UI/UX planning docs with the shipped frontend primitive-governance direction. `docs/UI_UX_WORLD_CLASS_ROADMAP.md` now explicitly treats the route-level shadcn-consistency cleanup as complete maintenance infrastructure rather than an active redesign stream, `docs/ux/LIFEOS_V1_UX_UPGRADE_PLAN.md` and `docs/ux/LIFEOS_V1_UX_SCORECARD.md` now require future UX work to reuse the app-local primitive layer instead of reintroducing route-local seams, and the older `docs/superpowers/specs/2026-06-03-lifeos-ui-ux-modernization-design.md` plus `docs/superpowers/plans/2026-06-03-lifeos-ui-ux-modernization-implementation.md` now mark themselves as historical inputs rather than the live execution queue. Added `docs/implementation-notes/2026-06-06-ui-ux-plan-shadcn-alignment.md`. This was a docs/planning alignment pass only; no runtime code, tests, or UX behavior changed.
- Completed the current shadcn-consistency route pass for repeated primitives and shared interaction seams. Added app-local `Label` and `Skeleton` primitives, moved `WorkflowLoadingState` onto the shared skeleton system, upgraded `DiagnosticsDisclosure` into the reusable system-details wrapper, replaced repeated raw label/disclosure markup across Home, Capture, Triage, Planning, Execute, Review, Health, Areas, and Google Calendar settings surfaces, and rebuilt `/login` onto the same card/form system instead of inline-styled HTML. Added `docs/implementation-notes/2026-06-06-shadcn-primitive-consistency-pass.md` and `docs/implementation-notes/2026-06-06-shadcn-consistency-plan-completion.md`. Validation passed with `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts`.
- Recorded the frontend primitive-governance decision for future agents. `AGENTS.md`, `docs/CODEX_SKILL_ROUTING.md`, and `docs/UI_UX_WORLD_CLASS_ROADMAP.md` now explicitly direct frontend work toward app-local shadcn-compatible primitives and shared patterns while keeping shell identity, route composition, area accents, and authored workflow surfaces custom. Added `docs/implementation-notes/2026-06-06-frontend-shadcn-governance.md`. This was a docs/governance pass only; no runtime code, tests, or UX behavior changed.
- Completed Pass 2C and effectively closed the shared visual-authorship buildout. Capture, Planning, and Triage now use the same explicit flagship/support/admin surface taxonomy as the rest of the primary workflow: Capture now keeps the writing surface as the only flagship while saved/device-only history becomes quieter support/admin depth, Planning now treats `Planning flow` as the single flagship while task/proposal/block surfaces stay secondary, and Triage now keeps the current item as the only flagship while queue/context/browser-note surfaces stay clearly secondary. Added `docs/implementation-notes/2026-06-05-visual-authorship-taxonomy-capture-planning-triage.md`. Validation passed with `pnpm lint`, `pnpm build`, `pnpm type-check` (after one rerun post-build because this repo still intermittently hits the known transient `.next/types` generation race), `pnpm test`, `pnpm --filter @lifeos/web test -- src/__tests__/capture.test.tsx src/__tests__/triage.test.tsx src/__tests__/workflowAreaAccent.test.tsx src/__tests__/phase4aPersistence.test.tsx src/__tests__/sourceOfTruth.test.ts`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-card-accent.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts tests/e2e/p0-ux-regression.spec.ts tests/e2e/execute-focus-flagship.spec.ts`.
- Completed the next Pass 2B visual-authorship slice. Review, Health, and Areas now use the same explicit surface taxonomy as Home and Execute instead of relying on older primary/secondary/quiet card drift: Review now has one closure-first flagship plus quieter reflection/history admin surfaces, Health now keeps the reliability answer as the single flagship with support/admin diagnostics demoted below it, and Areas now distinguishes flagship creation, support summaries, and quieter admin/reset surfaces without changing route truthfulness or any schema/auth/parser/calendar/persistence/auth boundary. Added `docs/implementation-notes/2026-06-05-visual-authorship-taxonomy-review-health-areas.md`. Validation passed with `pnpm lint`, `pnpm build`, `pnpm type-check` (after one rerun post-build because this repo still intermittently hits the known transient `.next/types` generation race), `pnpm test`, `pnpm --filter @lifeos/web test -- src/__tests__/healthPage.test.tsx src/__tests__/workflowAreaAccent.test.tsx src/__tests__/phase4aPersistence.test.tsx`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-card-accent.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts tests/e2e/p0-ux-regression.spec.ts tests/e2e/execute-focus-flagship.spec.ts`.
- Completed the first active Pass 2A visual-authorship slice. `globals.css` now defines an explicit shared surface taxonomy (`workflow-flagship-card`, `workflow-support-card`, `workflow-admin-card`, `workflow-shell-panel`) instead of relying on route-local styling alone, `WorkflowPageHeader` and `AppShell` now expose a calmer shell-context frame around that taxonomy, Home now presents its `Today / Next` surface as a true flagship panel with quieter support cards, and Execute now uses the same flagship/support split for the current mission, focus-state, and cleanup surfaces without changing read-only Home, Execute behavior, or any schema/auth/parser/calendar/persistence boundary. Added `docs/implementation-notes/2026-06-05-visual-authorship-taxonomy-home-execute.md`. Validation passed with `pnpm lint`, `pnpm build`, `pnpm type-check` (after one rerun post-build because this repo still intermittently hits the known transient `.next/types` generation race), `pnpm test`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-card-accent.spec.ts tests/e2e/execute-focus-flagship.spec.ts tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts`.
- Installed a durable UI/UX program-memory layer without changing runtime behavior: added `docs/UI_UX_WORLD_CLASS_ROADMAP.md` as the canonical active UX roadmap with the fresh-run protocol, five target outcomes, route scorecard, ordered pass queue, proof contract, and one explicit next pass; updated `docs/agent/CONTEXT_INDEX.md` plus `docs/agent/REPO_MAP.json` so `pnpm agent:context ui` now routes fresh agents through roadmap -> shipped state -> latest proof note instead of chat archaeology; and added `docs/implementation-notes/2026-06-05-ui-ux-roadmap-routing-spine.md`. Validation passed with `pnpm agent:context ui`, `pnpm exec prettier --check docs/UI_UX_WORLD_CLASS_ROADMAP.md docs/agent/CONTEXT_INDEX.md docs/agent/REPO_MAP.json docs/PROJECT_STATE.md docs/implementation-notes/2026-06-05-ui-ux-roadmap-routing-spine.md`, and `git diff --check`. No runtime, schema, auth, parser, calendar, or persistence behavior changed.
- Completed the latest shipped IA/polish correction pass across Home, AppShell, Capture, Execute, Review, Areas, Triage, Planning, and Health. Home is strict read-only again and no longer exposes route-local quick capture, the shell no longer shows quick-note mutation on `/`, Capture now demotes device-only draft/history helpers behind calmer disclosures, Execute now removes the redundant empty-state explainer card and keeps mission/recovery detail secondary, Review now keeps closure and summary visible while moving reflection/history/detail behind disclosures, Areas now keeps only immediate area actions visible while hiding accent/destructive/admin detail behind calmer reveals, Triage no longer duplicates empty-state guidance, Planning no longer repeats the empty-state CTA already provided by `Planning flow`, Health no longer duplicates healthy-state success feedback, and route-level diagnostics now use route-specific disclosure labels instead of repeating the same generic label everywhere. Updated proof in `src/__tests__/page.test.tsx`, `src/__tests__/sourceOfTruth.test.ts`, `src/__tests__/healthPage.test.tsx`, `src/__tests__/capture.test.tsx`, `src/__tests__/phase4aPersistence.test.tsx`, `tests/e2e/p0-ux-regression.spec.ts`, `tests/e2e/workflow-card-accent.spec.ts`, `tests/e2e/execute-focus-flagship.spec.ts`, and added `docs/implementation-notes/2026-06-05-home-review-areas-shell-polish-pass.md`. Validation passed with `pnpm lint`, `pnpm type-check`, `pnpm test`, `pnpm build`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts tests/e2e/interaction-feedback.spec.ts tests/e2e/workflow-card-accent.spec.ts tests/e2e/execute-focus-flagship.spec.ts`.
- Completed the Home/Execute information-architecture reduction pass. Home still keeps one dominant next action and one visible support surface, but now also previews secondary lists instead of dumping them, moves extra route choices into quieter disclosures, and tucks follow-through guidance behind one compact reveal. Execute now keeps the mission visible while moving next-action guidance, side-thought capture, and deeper execution detail into calmer disclosures/trays instead of showing every support surface at once. Added shared `workflow-inline-disclosure` and compact preview-list primitives in `globals.css`, updated focused proof in `src/__tests__/page.test.tsx`, `src/__tests__/executeFocusPolish.test.tsx`, `src/__tests__/routeSmoke.test.tsx`, and kept browser proof clean with `tests/e2e/p0-ux-regression.spec.ts`, `tests/e2e/workflow-hierarchy.spec.ts`, and `tests/e2e/interaction-feedback.spec.ts`. Added `docs/implementation-notes/2026-06-04-home-execute-ia-reduction-pass.md`.
- Completed the Triage premium-feel pass. Triage now opens with a stronger `One decision at a time` header, exposes calmer queue/save metrics, upgrades `Current focus` into a clearer next-move surface, and turns acceptance feedback into a stronger handoff toward Planning without changing queue semantics, save boundaries, or browser-note honesty. Updated focused proof in `src/__tests__/triage.test.tsx` and kept browser proof clean with `tests/e2e/p0-ux-regression.spec.ts` plus `tests/e2e/workflow-hierarchy.spec.ts`. Added `docs/implementation-notes/2026-06-04-triage-premium-feel-pass.md`.
- Completed the Capture/Planning premium-feel pass. Capture now uses a stronger `Raw first` header spotlight, a more decisive save-action tray, and celebratory but still truthful save/organize confirmations. Planning now opens with a stronger `Local-first planning` header, a single guided `Planning flow` surface, and calmer metric framing for what needs time, what is ready to review, and what is already planned. Updated focused proof in `src/__tests__/capture.test.tsx` to the newer structured `Sorting help` contract and added `docs/implementation-notes/2026-06-04-capture-planning-premium-feel-pass.md`. Validation passed with focused Vitest suites, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/interaction-feedback.spec.ts tests/e2e/workflow-hierarchy.spec.ts`, and the full repo bar (`pnpm lint`, `pnpm test`, `pnpm build`, `pnpm type-check`).
- Completed the latest UI modernization batch across Capture, Triage, Planning, Review, Health, and Areas. `/capture` now starts with the writing surface and a clearer area landing panel, `/triage` uses a current-focus queue instead of a backlog feel, `/calendar` groups work by planning intent (`Needs a suggested time`, `Ready to review`, `Already planned`), `/review` is closure-first, `/health` now shows `No active warnings` when appropriate, and `/settings/areas` uses a quieter admin header while preserving save-mode and diagnostics truth. Updated focused coverage in `src/__tests__/capture.test.tsx`, `src/__tests__/phase4aPersistence.test.tsx`, `src/__tests__/sourceOfTruth.test.ts`, `src/__tests__/healthPage.test.tsx`, `src/__tests__/workflowAreaAccent.test.tsx`, `tests/e2e/interaction-feedback.spec.ts`, `tests/e2e/p0-ux-regression.spec.ts`, and `tests/e2e/areas-color-edit.spec.ts`.
- Completed the shared shell polish pass. `globals.css`, `AppShell`, and `WorkflowPageHeader` now provide a more composed visual frame with stronger page rhythm, calmer chrome, better disclosure treatment, and lighter interaction polish, while Review, Health, and Areas now consume the stronger shared header primitive without changing workflow behavior. Added `docs/implementation-notes/2026-06-03-shared-shell-polish-pass.md`. Validation passed with focused Vitest suites, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts`, `pnpm lint`, `pnpm test`, `pnpm build`, and `pnpm type-check` (after rerunning once post-build because this repo still occasionally hits the transient `.next/types` generation race).
- Completed the Home/Execute declutter pass. Home now shows one support card by default and moves the rest behind one disclosure, but keeps Quick Capture physically pinned while the user is typing or viewing save feedback so the card does not jump away mid-flow. Execute now keeps the current mission primary while moving mission metadata, execution/save-mode detail, and recent-session detail behind calmer disclosures. Added `docs/implementation-notes/2026-06-03-home-execute-declutter-pass.md`. Validation passed with focused Vitest suites, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts`, `pnpm lint`, `pnpm test`, `pnpm build`, and `pnpm type-check`.
- Completed the Triage/Planning declutter pass. Triage removed the extra backlog-warning card, keeps only accept/reject/edit visible on the current item, and moves AI context, draft notes, browser-note actions, and deeper queue items behind disclosures. Planning now shows only the next task needing time, the next suggested proposal, and a short visible slice of planned blocks by default, with additional tasks/proposals/blocks moved behind disclosures; Google first-write approval remains explicit inside Google options and proposal reject moved into a quieter `More options` path. Added `docs/implementation-notes/2026-06-03-triage-planning-declutter-pass.md`. Validation passed with focused Vitest suites, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts tests/e2e/workflow-hierarchy.spec.ts`, `pnpm lint`, `pnpm test`, `pnpm build`, and `pnpm type-check` (after one rerun because this repo still intermittently hits the transient `.next/types` generation race).
- Hardened hierarchy proof for Triage and Planning. `tests/e2e/workflow-hierarchy.spec.ts` now seeds deterministic Triage queue and Planning route state, proves `/triage` keeps the current item dominant while `Current focus` and `Waiting after this` stay secondary, proves `/calendar` keeps `Needs a suggested time` dominant while `Planning flow`, `Ready to review`, and `Already planned` stay quieter, and extends the 390px overflow proof to both routes. Added stable hierarchy test ids/classes in `src/app/triage/page.tsx` and `src/app/calendar/page.tsx`.
- Hardened the Playwright E2E wrapper against cold-route compile failures in this Windows + OneDrive workspace. `scripts/run-playwright-e2e.mjs` now prewarms `/`, `/capture`, `/triage`, `/calendar`, `/execute`, `/review`, `/settings/areas`, `/health`, and `/api/parse-capture` before Playwright starts, and `tests/e2e/p0-ux-regression.spec.ts` now follows the shipped Home/Triage flow more directly. Added `docs/implementation-notes/2026-06-03-playwright-route-warmup.md`. Validation passed with `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-hierarchy.spec.ts`, `pnpm lint`, `pnpm type-check`, `pnpm test`, and `pnpm build`.
- Completed the Review/Health/Areas premium polish pass. `Review` now uses cleaner reflection/action separation plus composed metric cards instead of a raw counts list, `Health` now frames trust summary and repair focus with calmer premium support surfaces, and `Areas` now separates everyday area actions from destructive controls while giving the route a stronger spotlight summary. Added shared polish primitives in `src/app/globals.css` and documented the pass in `docs/implementation-notes/2026-06-03-review-health-areas-premium-polish.md`. Focused proof passed with `pnpm --filter @lifeos/web test -- src/__tests__/phase4aPersistence.test.tsx src/__tests__/healthPage.test.tsx src/__tests__/workflowAreaAccent.test.tsx` and browser proof passed with `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-hierarchy.spec.ts tests/e2e/p0-ux-regression.spec.ts`.
- Hardened the Playwright E2E wrapper against false local-server attachment on Windows: `scripts/run-playwright-e2e.mjs` now chooses an isolated port by default and errors immediately if the spawned Next process dies before readiness, and `tests/e2e/areas-color-edit.spec.ts` now waits for the real Areas route surface instead of clicking into a dev-time compile 404.
- Completed issues `#131`, `#133`, `#135`, `#136`, and `#137` with one shared workflow UI polish batch instead of five disconnected screen edits. The batch stayed route-only: `/triage`, `/calendar`, `/execute`, `/review`, and `/settings/areas` now use the shared calm loading treatment, empty states expose direct recovery/start actions, card hierarchy is clearer, Triage browser-local note actions are grouped and explicitly labeled as note-only, and Review now captures saved reflection text in addition to numeric summary counts. Added/updated focused coverage in `src/__tests__/sourceOfTruth.test.ts`, `src/__tests__/triage.test.tsx`, `src/__tests__/phase4aPersistence.test.tsx`, and `tests/e2e/p0-ux-regression.spec.ts`. Validation passed with `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, `pnpm --filter @lifeos/web build`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts`.
- Completed issue `#123` with a narrow Capture source-of-truth fix instead of a provider rewrite. `/capture` now separates saved history from device-only organized captures, loads durable saved rows through `listCaptureItems` when Supabase is active, immediately appends newly saved captures into that saved-history surface, and keeps device-only organize results in the existing workflow-context section. Seeded default areas and custom persisted areas now both filter correctly because the page matches workflow area ids against persisted area ids instead of comparing them directly. Added focused coverage in `src/__tests__/capture.test.tsx` for seeded/custom area mapping and save-surface behavior, kept existing provider wiring/persistence/accent/source-of-truth suites green, added `docs/implementation-notes/2026-06-02-capture-history-source-of-truth.md`, and validated with `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test -- capture routeSmoke workflowAreaAccent phase4aPersistence sourceOfTruth`, `pnpm --filter @lifeos/web build`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts`.
- Completed issues `#124` and `#128` with a browser-regression hardening batch. `tests/e2e/p0-ux-regression.spec.ts` now checks 390px overflow on all primary workflow routes and adds keyboard reachability smoke for Triage, Planning, Execute, Review, and Areas using realistic route fixtures instead of header-only coverage. `tests/e2e/areas-color-edit.spec.ts` now covers every shipped area-accent preset in dark mode, verifies selected-state text is not color-only, checks keyboard-visible focus on preset controls, and prompted a real selected-area control contrast fix on `/settings/areas` so the chosen-area action no longer depends on a low-contrast tinted fill.
- Completed planning issues `#132`, `#134`, `#138`, and `#139` with `docs/implementation-notes/2026-06-02-interaction-planning-decisions.md`. The note rejects a fake generic undo layer, defers a command palette, defines a later inline manual-time-edit model for Planning proposals, and explicitly recommends against mobile bottom navigation for current V1 while header density, label authority, and mobile route coverage are still stabilizing.
- Completed planning issues `#126`, `#127`, `#129`, and `#130` with `docs/implementation-notes/2026-06-02-ux-ia-and-scope-decisions.md`. The note keeps Settings on one V1 route split into `Area management` / `Integrations` / `Local data`, defines the docs-first canonical label map (`Today`, `Capture`, `Triage`, `Planning`, `Execute`, `Review`, `Health`, with `Areas` kept secondary/admin), recommends a Today-plus-Areas first-run path instead of a wizard, and documents that selected area is currently an orientation/default-assignment layer rather than a global truthy filter across all core routes.
- Completed issues `#120`, `#121`, `#122`, and `#125` with a narrow AppShell/Home clarity pass. AppShell now removes the leftover `Session workflow area` wording from primary UI, collapses duplicate quick-capture save-scope chrome into one plain-language line, and adds a working `Skip to main content` path that focuses the main content target without exposing diagnostics internals. Home's primary `Next` CTA now maps to the actual next-action kind instead of the generic `Open next step`. Validation passed with focused Vitest suites (`appShellAccent`, `page`, `routeSmoke`, `sourceOfTruth`), `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web test:e2e -- tests/e2e/app-shell-accent.spec.ts`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts`.
- Completed issue `#114` and refreshed the broad UX smoke proof instead of leaving it stale: `tests/e2e/p0-ux-regression.spec.ts` is now aligned with the shipped Home/Capture/Triage/Review labels and focused keyboard path, and the broad smoke suite passed 12/12 again. The existing area-accent, mobile-overflow, keyboard reachability, and color-not-only-signal checks are now current proof rather than historical intent. Validation passed with `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts`.
- Closed issue `#113` as already satisfied by the current Home/Today implementation on `main`: the route already has one dominant next-action panel, selected-area visibility through text plus accent treatment, suppressed irrelevant/empty sections, preserved read-only behavior, and narrow-width proof from the earlier Home/mobile/accent batches. No additional runtime changes were needed in this run beyond keeping the browser smoke suite truthful.
- Completed issue `#111` with a narrow interaction-feedback pass instead of a notification-system detour. The affected routes already had inline status/alert patterns, so the fix stayed inside those patterns and upgraded the generic `Saved` confirmations into plain-language `what happened / where next` feedback. The main changes are on `/triage`, `/calendar`, `/execute`, and `/review`, plus a small local-Planning follow-up so mock/device-only `Plan this time` and `Reject` actions emit the same explicit feedback as persisted actions. Added focused coverage in `src/__tests__/phase4aPersistence.test.tsx`, kept route-state proof in `src/__tests__/executeFocusPolish.test.tsx`, added a dedicated browser path in `tests/e2e/interaction-feedback.spec.ts` covering Capture -> Triage -> Planning -> Execute -> Review feedback states, and refreshed `tests/e2e/p0-ux-regression.spec.ts` so the broader suite stays aligned with the shipped UI. Validation passed with focused Vitest suites, the dedicated Playwright path, in-app browser open/screenshot proof on `/review`, `pnpm lint`, `pnpm type-check` (after rerunning once post-build because `.next/types` raced during the parallel validation start), `pnpm test`, and `pnpm build`. `pnpm format:check` still fails on the repo's broad pre-existing formatting drift and remains a separate cleanup stream.
- Completed issue `#115` as a documented review decision rather than a premature rename pass: current authority docs still define the six primary workflow screens as `Capture`, `Triage`, `Calendar / Planning`, `Execute`, `Review`, and `Health`, while the UX upgrade plan only suggests lighter future-facing labels such as `Focus` or a later `Triage` review. Because that is not enough authority for runtime renames, label changes were deferred and recorded in `docs/implementation-notes/2026-05-28-navigation-label-decision.md` instead of creating a docs/runtime mismatch or route-smoke churn.
- Completed issue `#109` with a safe area-color editing batch on `/settings/areas`: the app already had persisted `areas.color` support in the migration, schema, row selection, and workflow area sync path, so the missing piece was the update contract and UI. Areas cards now expose preset accent swatches plus a `Default` reset path, preview changes immediately on-card, and propagate the selected area's updated accent through the shell when the area is selected, without adding migrations, RLS changes, or dependencies. Added the shared update contract in `packages/schemas/src/index.ts`, the persisted/mock `updateAreaColor` helper in `src/lib/data/workflow.ts`, focused settings persistence coverage in `src/__tests__/phase4aPersistence.test.tsx`, helper coverage in `src/lib/data/workflow.test.ts`, and rendered proof in `tests/e2e/areas-color-edit.spec.ts`. Validation passed with focused schema/data/settings suites, `pnpm lint`, `pnpm type-check`, `pnpm test`, `pnpm build`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/areas-color-edit.spec.ts`.
- Completed issue `#112` with a route-only Focus flagship polish on `/execute`: the page now leads with a state-specific focus banner that makes the current state and next move obvious for ready/running/paused/terminal sessions without changing persistence or session semantics. Current area treatment is stronger and text-visible, first tiny step and definition of done stay prominent on the mission card, side-thought capture moved into a clearly secondary support panel, and terminal recovery guidance now points cleanly toward capture/review/re-planning without shame wording or fake resume behavior for persisted paused sessions. Added focused execute coverage in `src/__tests__/executeFocusPolish.test.tsx` and rendered proof in `tests/e2e/execute-focus-flagship.spec.ts`. Validation passed with focused Vitest route/persistence/source-of-truth suites, `pnpm lint`, `pnpm type-check`, `pnpm test`, `pnpm build`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/execute-focus-flagship.spec.ts`. `pnpm format:check` still reports the repo's pre-existing global formatting drift, and the broader `tests/e2e/p0-ux-regression.spec.ts` still has older unrelated stale copy/selector failures outside this Execute batch.
- Completed issue `#110` with a hierarchy-only workflow polish batch: Home, Capture, Triage, Execute, Review, Areas, and the Health top summary now use shared primary/secondary/support card treatments so each touched screen has one visually dominant current-state or next-action surface instead of equal-weight card stacks. The pass keeps behavior unchanged, pushes supporting context/empty states quieter, preserves dark-mode coherence, and avoids overlapping Planning proposal-state work beyond shared visual primitives. Added shared hierarchy classes in `src/app/globals.css`, focused hierarchy assertions in `src/__tests__/workflowAreaAccent.test.tsx` and `src/__tests__/healthPage.test.tsx`, plus rendered proof in `tests/e2e/workflow-hierarchy.spec.ts`. Validation passed with focused Vitest route suites, `pnpm lint`, `pnpm type-check`, `pnpm test`, `pnpm build`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-hierarchy.spec.ts`. `pnpm format:check` still reports the repo's pre-existing global formatting drift and was treated as non-blocking for this scoped UI batch.
- Completed issue `#108` with a focused workflow-card accent batch: the selected/current area accent now extends past `AppShell` into the core workflow surfaces without changing behavior. Home `Next`, Capture save-options and recent-capture cards, Triage current-item, Planning proposal and scheduled-block cards, Execute current-mission, Review summary/carry-forward, and Areas cards now reuse the shared area-accent token system with visible area chips or labels so color is never the only signal. Execute gets the strongest treatment, while Planning/Calendar behavior, approval gating, lifecycle chips, dark-mode readability, and empty-state restraint remain intact. Added shared accent helpers in `src/lib/areaAccent.ts`, focused route regression coverage in `src/__tests__/workflowAreaAccent.test.tsx`, and rendered proof in `tests/e2e/workflow-card-accent.spec.ts`. Validation passed with focused Vitest route suites, `pnpm lint`, `pnpm type-check`, `pnpm test`, `pnpm build`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/workflow-card-accent.spec.ts`.
- Resolved issue `#87` as the final Planning/Calendar trust-language pass: `/calendar` now distinguishes tasks that still need time from suggested time blocks and planned blocks, conflict state is phrased in plain language (`Calendar not checked`, `Calendar looks open`, `Calendar conflict found`), Google Calendar actions are visually secondary behind a `Google Calendar options` disclosure unless connected or otherwise relevant, and disabled reasons no longer leak `Supabase` or proposal-status internals into primary copy. The Google event create success path also no longer relies on brittle React state-updater side effects to “confirm” a successful write after the server already returned success. Validation passed with `pnpm --filter @lifeos/web test -- phase4aPersistence.test.tsx sourceOfTruth.test.ts routeSmoke.test.tsx`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web build`. No schema, migration, RLS, OAuth scope, service-role, parser, observability, or Google Calendar capability changes were introduced. See `docs/implementation-notes/2026-05-28-planning-calendar-clarity.md`.
- Completed issue `#107` with a narrow shell-only UI batch: `AppShell` now exposes a subtle selected-area accent line, brand dot, active-nav treatment, and current-area surface styling driven by the selected area's existing color while preserving explicit text labels and neutral fallback behavior. Active nav state is now marked with `aria-current="page"` so the shell does not rely on color alone, and the current-area selector/context surfaces now reflect the selected area without broad route or card refactors. Added focused regression coverage in `src/__tests__/appShellAccent.test.tsx` plus a dedicated rendered Playwright spec `tests/e2e/app-shell-accent.spec.ts` proving live area switching, active-nav semantics, and no horizontal overflow at `390px`. Validation passed with focused Vitest shell tests, `pnpm lint`, `pnpm type-check`, `pnpm test`, `pnpm build`, and `pnpm --filter @lifeos/web test:e2e -- tests/e2e/app-shell-accent.spec.ts`.
- Completed the first LifeOS V1 UX upgrade foundation batch for issues `#105`, `#106`, and `#116` without runtime changes: added [`docs/ux/LIFEOS_V1_UX_UPGRADE_PLAN.md`](docs/ux/LIFEOS_V1_UX_UPGRADE_PLAN.md) to define the next product-experience layer, its constraints, area-accent token vocabulary, default area palette direction, screen-level goals, sequencing, and PR completion checklist; and added [`docs/ux/LIFEOS_V1_UX_SCORECARD.md`](docs/ux/LIFEOS_V1_UX_SCORECARD.md) as the short reusable review rubric for trust clarity, next-action clarity, area visibility, mobile/accessibility, keyboard/focus behavior, and explicit external-write safety. This batch is intentionally docs-only and unblocks later `area:ui` implementation work without changing schemas, routes, persistence, auth, parser, observability, or calendar behavior.
- Completed scoped Area management hardening: `/settings/areas` now lets authenticated users create Areas directly, remove Areas through soft delete only, and recover cleanly from the zero-active-area state with an immediate create path instead of a dead end. The shared workflow provider and Capture/Triage area mapping now also handle custom persisted Areas without relying on the original seeded slug map, removed Areas stay out of active pickers/new assignment, and persisted Review history still renders safely with a fallback label when an old `area_id` no longer belongs to an active Area. Added a follow-up migration that revokes authenticated hard delete for `areas` and route/data/provider regression coverage for create, soft-delete, zero-area onboarding, custom-area sync, and historical review fallback. Local opt-in RLS execution is still pending because local Supabase was unavailable from this machine (`dockerDesktopLinuxEngine` pipe missing). See `docs/implementation-notes/2026-05-28-area-management-soft-delete.md`.
- Resolved issues `#91` and the first safe slice of `#92` in one narrow UI batch: added a shared `workflowLifecycle` helper so Capture, Triage, Planning, Execute, and Review describe item state with the same lifecycle chips instead of route-specific labels, and added a visible `Ctrl/Cmd + Enter` keyboard shortcut for `Save thought` from the main Capture textarea only. The persisted Review regression that guards against mixing local capture state into Supabase review summaries was tightened to wait for the async provider swap instead of asserting too early. Validation passed with `pnpm --filter @lifeos/web test -- capture.test.tsx triage.test.tsx phase4aPersistence.test.tsx sourceOfTruth.test.ts routeSmoke.test.tsx`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web build`. No schema, migration, RLS, auth, parser, observability, or Google Calendar write behavior changed. See `docs/implementation-notes/2026-05-28-lifecycle-chips-and-capture-shortcut.md`.
- Resolved issues `#88`, `#89`, and `#90` in one route-only UX batch: Execute no longer floods terminal/not-started states with disabled controls, Review now groups the day into captured/planned/completed/follow-up/carry-forward summaries before raw detail, and Areas now acts like a real workspace routing surface with truthful per-area counts and actions while keeping slugs inside diagnostics only. Added a read-only `listCaptureItems` helper so persisted Review can summarize captures without mixing in local browser notes. Validation passed with `pnpm --filter @lifeos/web test -- phase4aPersistence.test.tsx routeSmoke.test.tsx sourceOfTruth.test.ts`, `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web type-check`, `pnpm --filter @lifeos/web test`, and `pnpm --filter @lifeos/web build`.
- Resolved issues `#84`, `#85`, and `#86` in one narrow UI batch: Home now keeps one dominant deterministic next action without rendering empty planning/recovery/system cards as equal clutter, Capture now explains save/organize outcomes more directly and translates recent capture statuses into human lifecycle labels, and Triage now shows one current review item with explicit up-next switching while preserving accept/edit/reject behavior. Focused Home/Capture/Triage tests were updated, the persisted triage project-acceptance regression was updated for the new queue behavior, and `pnpm --filter @lifeos/web lint`, `pnpm --filter @lifeos/web build`, `pnpm --filter @lifeos/web type-check`, and `pnpm --filter @lifeos/web test` all passed. No schema, persistence contract, auth, parser, or Google Calendar write behavior changed. See `docs/implementation-notes/2026-05-27-home-capture-triage-queue-clarity.md`.
- Resolved issue `#95` by aligning the shared workflow provider with persisted area truth: when signed-in Supabase areas load successfully, `WorkflowProvider` now replaces the old Phase 2 mock area list with the persisted active-area set, resets the selected area if the old session-only id is no longer valid, and clears the selector cleanly when persisted storage has zero active areas so AppShell no longer implies mock areas exist while `/settings/areas` says none do. Focused provider/shell regression tests plus `pnpm --filter @lifeos/web type-check` and `pnpm --filter @lifeos/web lint` passed. No auth, RLS, parser, calendar-write, or mock-fallback behavior changed.
- Closed issue `#79` at the planning layer only: added `docs/implementation-notes/2026-05-27-production-readiness-truth-packet.md` to separate real production env/config requirements from UX copy debt, restate the required Vercel env matrix for persisted behavior / AI sorting / Google Calendar, classify `life-os-web-azure.vercel.app` as a deployed fallback candidate rather than proven production-ready behavior from current repo evidence, and recommend authenticated production smoke before more broad trust-copy cleanup is treated as done. No runtime, auth, env, secret, deployment, or Google Calendar behavior changed.
- Resolved the shared local persisted Supabase `/login` and `/settings/areas` failure path behind issues `#93` and `#94`: browser-visible Supabase config now stays on static `NEXT_PUBLIC_*` lookups so Next can inline it into the client bundle, persisted Supabase rows now normalize provider datetime offsets before schema validation at the data boundary, and fresh local smoke again reaches `/settings/areas` with persisted areas visible (`Main Job`, `Personal`, `Volunteer Work`, `Side Project`) and without either `Supabase is not configured` or `Areas could not load`. Focused regression coverage plus `pnpm lint`, `pnpm type-check`, `pnpm test`, and `pnpm build` passed. Production verification remains separate because Vercel deployment protection previously blocked direct `/login` smoke from this environment. See `docs/implementation-notes/2026-05-27-supabase-login-and-areas-fix.md`.
- Resolved issue `#76` by moving the repo-local Supabase port cluster out of the blocked Windows excluded TCP range (`54318-54417`) and updating local verification defaults/docs to the new API URL `http://127.0.0.1:15431`. Follow-up verification also confirmed the old analytics default port `54327` was inside the same blocked range, so the local-only fix moves the entire affected cluster to `15431-15439`. `supabase start`, `supabase db reset`, opt-in local RLS, the repo validation gates, and the existing browser regression suite now pass again. No runtime, RLS, auth, parser, calendar, or production env behavior changed. See `docs/implementation-notes/2026-05-27-windows-supabase-port-fix.md`.
- Closed the current verification/planning batch without runtime changes: local Supabase smoke proved a Windows environment blocker because the excluded TCP range `54318-54417` includes local Supabase DB port `54322`, so `supabase db reset` and the full local browser/RLS smoke could not complete from this machine state; production smoke reached the current Vercel deployment root successfully but authenticated route checks were blocked by deployment protection from this environment; and planning packets now exist for all-day conflict-check correctness and app-created Google Calendar update/cancel scope. See `docs/implementation-notes/2026-05-27-smoke-verification-results.md` and `docs/implementation-notes/2026-05-27-google-calendar-scope-plans.md`.
- Closed the daily-flow UX batch without changing workflow/data contracts: Home `/` now includes a first-run-safe `Daily loop` guide using existing routes only, Capture `/capture` now uses clearer mobile-friendly save/sort actions and more practical next-step copy, Triage/Planning/Execute copy now makes the next step more obvious, and Review `/review` now includes a simple close-the-loop action card plus a basic review log/history surface built from existing `review_entries` and local fallback notes only. No schema, migration, parser, auth, observability, env, or Google Calendar write behavior changed.
- Resolved the PostCSS security alert with a minimal dependency-policy change only: `pnpm-workspace.yaml` now overrides `postcss` to `^8.5.15`, `pnpm-lock.yaml` now resolves only `postcss@8.5.15` including the `next@15.5.18` path that had previously resolved `8.4.31`, and no direct unsafe user-controlled CSS embedding path was found in the app during the issue-driven search for direct PostCSS usage, `<style>` injection, or `dangerouslySetInnerHTML`-style CSS sinks. No runtime behavior, workflow permission, auth/calendar/parser/privacy boundary, or broad dependency set changed.
- Added the next governance-only automation planning layer without changing app runtime or enabling new merge behavior: `docs/agent/SCENARIO_PACKS.md` now defines T2 scenario-pack requirements and examples, `docs/agent/VERIFIED_MEDIUM_AUTOMATION.md` now defines a future policy-only verified-medium lane, `docs/agent/ISSUE_PLANNING_AUTOMATION.md` now documents when to use `agent:plan`, `.github/workflows/codex-issue-plan.yml` plus `.github/codex/prompts/issue-plan.md` now provide a read-only issue-planning packet workflow that comments repo-grounded research instead of opening branches/PRs, and the Agent Task template plus `docs/agent/CODEX_PROMPT_TEMPLATE.md` now require scenario-pack thinking for T2 workflow behavior without turning T0/T1 work into mandatory paperwork. No T2 auto-merge or runtime behavior was enabled.
- Hardened the hands-off agent-delivery control plane only: guard lists are now consolidated in `scripts/agent/automation-policy.mjs`, low-risk issue automation and CI autofix both use `scripts/agent/check-automation-scope.mjs` for deterministic forbidden-path checks, safe auto-merge label propagation from low-risk issue automation now requires an explicitly labeled source issue plus a post-validation docs/README/issue-template-only diff, PR evidence bundles now include route/coverage/validation/risk-surface/manual-proof fields, and workflow summaries now act as a lightweight automation decision log. New docs at `docs/agent/AUTOMATION_EVIDENCE_BUNDLE.md` and `docs/agent/AUTOMATION_DECISION_LOG.md` explain review and monthly reliability checks. No app runtime behavior, dependency graph, or auto-merge allowlist expansion changed.
- Added repo-local agent discipline hardening only for docs/control-plane work: `docs/agent/ANTI_RATIONALIZATIONS.md` now captures common shortcut excuses plus required reality checks, `.agents/skills/README.md` now defines the standard LifeOS repo-local skill anatomy, `skill-router` and `skill-security-review` now follow that anatomy, new repo-local skills now cover debugging, source-driven platform checks, contract review, and shipping proof, `.github/ISSUE_TEMPLATE/agent-task.yml` now includes task-surface validation presets, and the Codex PR-review prompts now call out rationalization, scope drift, PR-authored approval claims, and weak proof more explicitly. No app runtime, workflow YAML, dependency, or external-write behavior changed.
- Added role-separated delivery governance only for agent workflow work: `docs/agent/ROLE_SEPARATED_DELIVERY.md` now makes research/spec/build/test/validation checkpoints explicit for medium/high-risk or cross-flow engineering tasks, `.github/ISSUE_TEMPLATE/agent-task.yml` now requires a concise research/spec checkpoint, `docs/agent/CODEX_PROMPT_TEMPLATE.md` now references those checkpoints, and PR review prompts now tell validators to compare diffs against issue acceptance criteria/checkpoints/forbidden changes when trusted issue context is available. This does not add runtime multi-agent behavior, workflows, vendors, or weaker gates.
- Normalized the repo-wide Prettier baseline in one mechanical pass across tracked app, docs, workflow, and `.playwright-mcp` files. Also hardened `apps/web/src/__tests__/sourceOfTruth.test.ts` to normalize whitespace before copy assertions so formatter line wraps do not create false negative governance failures. No product behavior changed.
- Completed governance hardening for agent/process rules only: added `docs/agent/CODEX_PROMPT_TEMPLATE.md` with a Verification Oracle, formalized implementation-note expectations in `docs/implementation-notes/README.md`, clarified T3/T4 planning-first and engineering automation boundaries, aligned Node guidance around `.nvmrc` / Node 22.13.0, blocked test-only PRs from safe auto-merge until a stronger assertion-preservation guard exists, treated `.github/codex/prompts/**` as human-reviewed control-plane files instead of safe docs metadata, and hardened merge-base-sensitive workflows to use full-history checkouts plus Node 24-capable GitHub action versions.
- Added safe PR auto-merge guard automation only: `.github/workflows/safe-automerge.yml` now evaluates pull requests on open/reopen/synchronize/label/draft-state changes, enables GitHub auto-merge only when the PR is non-draft, carries both `automerge:safe` and `risk:low`, carries none of `risk:medium` / `risk:high` / `needs:human-decision`, and changes only docs/README/issue-template paths. The workflow writes exact eligibility reasons to the run summary, attempts to enable `SQUASH` auto-merge for eligible PRs only when the repository-level GitHub auto-merge setting is enabled, and disables previously armed auto-merge when the PR becomes unsafe. Deterministic path evaluation lives in `scripts/agent/check-safe-automerge.mjs`.
- Added explicit human-gate policy for agent automation: `.github/AGENT_AUTOMATION_POLICY.md` now defines T0-T4 automation tiers for docs/issue templates, low-risk PR work, review-required implementation and control-plane files, approval-gated risky surfaces, and human-decision-only scope changes; `AGENTS.md` now points future agents at that policy.
- Added Codex CI autofix automation only: `.github/workflows/codex-ci-autofix.yml` now listens to failed `workflow_run` completions from `CI`, skips `main`, skips existing `codex/ci-autofix-*` branches, rejects same-repo-ineligible runs, exports trusted prompt/context copies from the default branch before checking out the failing ref, runs Codex in a `workspace-write` sandbox, validates with `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm type-check`, `pnpm test`, `pnpm build`, `pnpm format:check`, and `git diff --check`, and opens a repair PR titled `codex/ci-autofix-<run_id>` against the original failing branch when safe. Supporting prompt instructions now live in `.github/codex/prompts/ci-autofix.md`. No auto-merge is enabled.
- Fixed low-risk issue automation reliability on GitHub Actions: `.github/workflows/codex-low-risk-issue-to-pr.yml` now reads `workflow_dispatch` issue numbers from the event payload instead of `core.getInput(...)` inside `actions/github-script`, uses a non-reserved parsed output key so `ready` / `no_changes` / `needs_human_review` branches actually execute after Codex returns JSON, and keeps the root `pnpm-lock.yaml` importer in sync with `package.json` for `@playwright/test` so `pnpm install --frozen-lockfile` succeeds in CI and eligible low-risk issue runs.
- Tightened the low-risk issue operating path for docs-only tasks: the bounded Codex prompt may now infer a single obvious existing doc target for repo-level command clarification when exactly one repo-owned surface already contains the referenced command set, and the Agent Task issue template now explicitly asks docs tasks to name the target doc and exact wording or ordering.
- Made low-risk workflow formatting checks non-blocking like CI: `.github/workflows/codex-low-risk-issue-to-pr.yml` now warns instead of failing when repo-wide `pnpm format:check` baseline drift is unrelated to the proposed low-risk patch, so otherwise-valid low-risk automation can still complete.
- Verified the live low-risk issue trigger path on GitHub with smoke-test issue `#30`: adding only `risk:low` triggered the workflow and cleanly skipped after `Resolve issue scope`, adding `agent:ready` triggered the eligible path into `Generate low-risk patch`, and adding `needs:human-decision` triggered the negative path with downstream jobs skipped as intended.
- Added low-risk Codex issue-to-PR automation only: `.github/workflows/codex-low-risk-issue-to-pr.yml` now watches labeled/edited issues plus manual `workflow_dispatch`, fetches the live issue state, runs only for open issues labeled `agent:ready` and `risk:low` while blocking `needs:human-decision`, `risk:medium`, and `risk:high`, and splits the flow into a read-scoped Codex generation job plus a separate write-scoped validation/PR job. The workflow checks out `main`, uses a deterministic branch name `codex/issue-<n>-low-risk`, feeds Codex a bounded prompt from `.github/codex/prompts/low-risk-implementation.md`, enforces forbidden-path guards, validates with `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm type-check`, `pnpm test`, `pnpm build`, `pnpm format:check`, and `git diff --check`, creates or updates a PR to `main` when changes pass, and comments back on the issue when no changes are needed or human review is required. It does not enable auto-merge.
- Added Codex PR review escalation protocol only: `.github/workflows/codex-pr-review.yml` now splits review into a deterministic `classify-risk` job, a default baseline review on `gpt-5.4-mini` medium, and a conditional escalated review on `gpt-5.4` high when risky labels/paths/size thresholds or the baseline reviewer request it. The workflow remains review-only, skips draft PRs, now also skips bot-authored PRs that the review action cannot legally serve, adds `labeled`/`unlabeled` triggers, keeps `safety-strategy: drop-sudo`, uses read-only Codex sandboxes, and posts separate baseline vs escalated PR review comments without push/patch behavior. Supporting assets now live in `.github/codex/prompts/pr-review-baseline.md`, `.github/codex/prompts/pr-review-escalated.md`, `scripts/agent/classify-pr-risk.mjs`, and `docs/agent/PR_REVIEW_ESCALATION_POLICY.md`.
- Added the initial GitHub Actions CI workflow at
  `.github/workflows/ci.yml`: it runs on pull requests and pushes to `main`
  with least-privilege `contents: read`, pins Node 22.13.0, uses pnpm cache
  via `actions/setup-node`, and validates the monorepo with
  `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm type-check`,
  `pnpm test`, `pnpm build`, and a non-blocking `pnpm format:check` warning
  without changing runtime behavior or adding deployment/secrets.
- Completed Phase 7A durable browser-regression hardening for P0 UX trust journeys: added Playwright config and stable accessible-locator tests in `apps/web/tests/e2e/p0-ux-regression.spec.ts` covering quick note feedback, capture save/save+organize, triage edit/note/accept-to-planning path, health run-check readiness states, areas reset cancel/confirm, 390px overflow checks on core routes, execute empty-state CTA, and keyboard tab reachability through primary controls.
- Completed Today/Home Cockpit v1 on `/` with a pure selector layer (`apps/web/src/lib/today/buildTodayCockpitModel.ts`) and scoped UI wiring (`apps/web/src/app/page.tsx`): Home now shows deterministic Now/Next, quick capture with truthful local-save messaging, needs-decision summaries, unplanned task routing, today planned blocks by local-day boundaries, non-shaming recovery signals, and Health link-first trust status; it does not create proposals, accept blocks, start sessions, run health persistence, or write Google Calendar events.
- Completed Issue #41 Home mobile UX pass only: `apps/web/src/app/page.tsx` now shortens Home cockpit copy, removes the empty-state Execute CTA from `Now`, reduces empty-card button noise, stacks multi-action rows safely on mobile, and deterministically prioritizes secondary cards based on the current `Next` recommendation. `apps/web/src/lib/today/buildTodayCockpitModel.ts` trims verbose Home-only action copy without changing selector logic or write boundaries, and `apps/web/src/__tests__/page.test.tsx` now asserts the updated honest empty-state wording.
- Hardened Playwright command teardown for this workspace without changing app runtime behavior: `scripts/run-playwright-e2e.mjs` now owns Next dev startup/readiness/cleanup for `apps/web` E2E runs, `apps/web/package.json` points `test:e2e` at that wrapper, and `apps/web/playwright.config.ts` disables its built-in `webServer` only when the wrapper sets `PLAYWRIGHT_DISABLE_WEBSERVER=1`. This keeps `next dev` as the exercised runtime path while avoiding the previous post-success hang from the standard repo command.
- Added cockpit regression coverage: new model unit tests (`apps/web/src/__tests__/todayCockpitModel.test.ts`), updated Home route tests (`apps/web/src/__tests__/page.test.tsx`), route smoke coverage for Home provider wiring (`apps/web/src/__tests__/routeSmoke.test.tsx`), source-of-truth boundary assertions for Home write-safety (`apps/web/src/__tests__/sourceOfTruth.test.ts`), and focused Home Playwright journeys in `apps/web/tests/e2e/p0-ux-regression.spec.ts`.
- Fixed P0 PR-B Health readiness and dead-feel behavior: `/health` now fails loading with a deterministic timeout path instead of waiting indefinitely, shows plain-language run-state feedback near `Run system check` (running/success/failure), keeps disabled-button rationale visible while loading, and adds regression coverage for timeout fallback plus run feedback without changing deterministic health scoring or observability-provider behavior.
- Completed scoped P1 UI hardening for shell/loading/icon noise without behavior changes: theme toggle now shows an explicit nearby loading reason while temporarily disabled during hydration, route loading copy now states these checks are background/non-blocking, and `/favicon.ico` now resolves through an app-route redirect to `/icon.svg` to remove favicon 404 console noise.
- Fixed P0 Capture action truthfulness end-to-end on `/capture` and AppShell quick note: quick note now fails visibly (no silent no-op), `Save thought` now explicitly states the storage destination versus browser-only recent-capture list behavior, and save-only captures now expose a one-click `Organize this saved thought` path that routes to the existing triage flow with visible `Review it now` CTA; parser failure safety and raw-capture persistence behavior remain unchanged.
- Fixed P0/P1 triage truthfulness gaps: task-card note-only actions now show visible on-card feedback (`Draft notes` + `Note saved in this browser`) instead of hidden-only mutation, triage edit now marks AI clarity notes as based on the original capture after title/description edits, and mock parser fallback now emits `project_draft` for clearly project-shaped captures so project acceptance is reachable in local/demo parsing paths.
- Fixed P0 triage-to-planning continuity for accepted tasks: `/calendar` now treats accepted active tasks as explicit **Unplanned tasks** in both Saved workspace and Demo mode, exposes a real `Draft a time block` action from that task list, and no longer dead-ends to planning empty state when an accepted task exists without a prebuilt proposal.
- Fixed P0 mobile AppShell overflow on narrow screens: header controls now stack/wrap safely, primary nav wraps instead of forcing horizontal width, and area-picker/clock controls move to responsive rows so the shell does not cause horizontal overflow at 390px while preserving existing route labels, behavior, and accessibility semantics.
- Added Playwright MCP agent-routing and validation guidance (`docs/CODEX_SKILL_ROUTING.md`, `docs/agent/PLAYWRIGHT_MCP_VALIDATION.md`, `docs/agent/VALIDATION_MATRIX.md`) so future agents have explicit triggers, non-triggers, required UI journeys, required handoff output format, and safety limits (local/dev-only preference, mock/demo preference, no unapproved external writes).
- Added `docs/VERCEL_PRODUCTION_CHECKLIST.md` with a production-focused Vercel env matrix, deploy prerequisites, post-deploy smoke order, stop-ship failures, and rollback sequence grounded in the current shipped Supabase/OpenAI/Google behavior.
- Added a production/Vercel rollout note to `README.md` and clarified `.env.example`: the repo does not use a single demo-mode master flag, production behavior depends on complete Supabase/OpenAI/Google env configuration, observability remains optional, and persisted `/execute` timing limits remain an intentional truthful boundary.
- Completed PR3 Execute guided-session feedback rework (UI/test updates only): replaced misleading timer-style execute feedback with truthful session state copy (`Session started`, `In progress`, `Paused`, `Demo timer`, `Elapsed time is tracked after completion`), made one mission card dominant (task, area, first tiny step, definition of done, next recommended action), grouped controls into start/pause/resume vs end outcomes vs secondary actions, added recovery-focused end-session guidance (`What got in the way?`, `What should happen next?`, `Create smaller first move`, `Reschedule later`), and improved no-task execute empty-state CTAs to Planning/Capture while preserving existing persistence/mock behavior and execution contracts.
- Completed PR1 fake-feeling Capture/Triage control hardening (UI/test updates only): AppShell quick note now requires user-entered text and is clearly browser-local, Capture success states now distinguish saved vs sent-to-review outcomes with plain-language guidance and a direct Triage CTA, browser-local organize actions are renamed honestly, Triage edit now supports real inline title/description updates through existing local draft mutation, note-only actions are relabeled as note-only, and accepted task/project confirmations now provide next-step CTAs (Plan time/Capture another) without changing persistence or external actions.
- Completed PR2 planning control credibility pass on `/calendar` (UI/test updates only): proposal creation now labels next-hour blocks as a quick adjustable default, proposal cards now separate local planning/Google/admin actions, `Adjust time` inline controls (`Move 30 min later`, `Shorten`, `Extend`) replaced `Edit +30 min`, disabled Google actions now show immediate inline reasons (local-only/Supabase required/disconnected/conflict-check/approval prerequisites), and explicit approval copy remains near write actions.
- Completed PR4 primary UX jargon cleanup across workflow screens and regression guards: removed provider/mock/parser-heavy wording from primary cards/buttons/statuses, moved implementation identifiers behind disclosures (`System details` / `Developer details`), renamed conflict/write actions to user-facing language, and updated source-of-truth + route/capture/health/persistence tests to keep plain-language UX plus honest degraded-state behavior without changing functionality or data contracts.
- Completed the follow-up trust-language and diagnostics unification batch for issues `#80-#83`: AppShell now uses plain `Current area` language plus a concise device-only quick-capture trust chip, normal workflow screens now share a single collapsed `Diagnostics` pattern instead of separate `System details` / `Developer details` sections, save/sync wording now consistently uses `Saved to account` / `Saved on this device only`, and `/health` now leads with a daily trust answer plus separate user-facing rows for saving, AI sorting, and calendar connection before optional diagnostics. See `docs/implementation-notes/2026-05-27-ui-trust-language-and-health.md`.
- Completed PR3 primary-action/empty-state hardening across Home, Capture, Triage, Calendar, Execute, Review, Health, and Areas: clarified one dominant next action per surface, strengthened empty-state next-step guidance, and simplified user-facing copy (especially Capture organization availability) without changing persistence, schema, auth, parser, or calendar write logic.
- Completed PR3 Execute guided-session-state UX pass on `/execute` (UI/test updates only): removed fake timer semantics in persisted mode, surfaced explicit session-state/timing truth and next recommended action, made one mission card dominant with first tiny step and definition of done, regrouped controls into start/pause/resume vs end outcomes vs secondary actions with clear disabled reasons, and added recovery-first end-session prompts for stuck/distracted/missed without changing execution persistence contracts or workflow side effects.
- Fixed P0 Execute terminal contradictions (UI/test updates only): replaced ad hoc session-status inference with explicit execute UI states, made terminal statuses non-active by default, ensured `Start` disabled reasons stay visible, regrouped controls into primary control + end outcomes + recovery actions, and added regression coverage for demo stop coherence plus terminal missed next-step actions.
- Completed a dark-mode-first UI/UX modernization pass across shell and core screens (Home, Capture, Triage, Calendar, Execute, Review, Health, Areas, Login) with calmer copy, clearer primary actions, improved empty states, and less dominant technical/mock wording in primary UX.
- Completed PR2 token-compliance pass for Calendar/Execute/Review/Google Calendar settings panel: replaced hardcoded inline light-theme status styling with semantic tokenized Card/Alert/Badge patterns and preserved existing actions, copy semantics, and approval gating behavior.
- Completed PR1 action-truthfulness copy hardening (copy/test only): AppShell `Quick note` now discloses browser-local scope, Triage note-only actions no longer imply real defer/reassign workflow changes, and Execute `Stop` now clearly communicates demo-mode local behavior plus persisted-mode disable rationale.
- Added a tokenized theme foundation (`apps/web/src/app/globals.css`) and local shadcn-style UI primitives (`button`, `card`, `badge`, `alert`, `separator`, `input`, `textarea`, `select`) plus `ThemeProvider`/toggle wiring for consistent dark-first styling without changing workflow logic.
- Preserved existing source-of-truth and safety behavior during UI refactor: no schema/RLS changes, no AI parser contract changes, no calendar write gate changes, no secret/env handling changes, and no removal of mock/demo fallback paths.
- Added/updated compatibility fixes so route-smoke and source-of-truth tests still pass after shell/theme changes (AppShell provider placement, required route labels/copy guards, and safe test fallback for `matchMedia`).
- Completed Phase 9C execute/review trust fixes: persisted terminal execution updates now collect explicit end-session fields (outcome, actual duration, productivity rating, optional notes) before saving, and no longer inject hardcoded productivity/duration/notes defaults.
- Completed Phase 9D planning and Google approval failure-state hardening: `/calendar` now uses normalized actionable failure copy, explicit local-versus-Google write state messaging, and post-action local-state confirmation before success status; Google settings status handling now normalizes load/connect/disconnect failures; and Google connection/create-event routes now return sanitized safe failure text.
- Completed Phase 9E health-truthfulness and regression-net expansion: `/health` now marks optional disabled/disconnected/signed-out states as informational non-blocking checks, keeps real auth/data-read failures visible, normalizes health failure summaries to avoid raw sensitive error echoing, and expands page-level regression coverage for Calendar, Execute, Review, Health, and Settings failure/empty/provider-state surfaces.
- Updated persisted execute task context display to include definition of done when present.
- Updated review source isolation so persisted-mode summaries do not read local session capture counts or local review log entries.
- Added focused Phase 9C regression coverage in schema tests, workflow data-layer tests, and execute/review persistence UI tests.
- Added the missing `docs/phase-9/9A-hardening-usability-audit.md` audit artifact with scoped blockers/risks, targeted test gaps, and the Phase 9B-9E PR sequence.
- Completed Phase 9B source-of-truth clarity pass with copy-only changes in AppShell/Capture/Triage/Settings: local session workflow state vs persisted provider-backed data is now explicit, area-selection behavior is described consistently, and stale phase/future-work helper text was removed.
- Added focused regression coverage for Phase 9B messaging in route smoke/capture tests and source-of-truth static guards.
- Added compact agent context-orientation docs plus a bounded `pnpm agent:context <area>` helper for repo-maintenance and future coding-agent routing; this is docs/tooling only and does not change app runtime behavior.
- Added a canonical `docs/skills/next-phase-gate-review.md` review skill plus narrow `AGENTS.md` and Cursor-rule wiring so future agents run a diagnostic gate before new phases, risky-surface work, PR open, or merge; the review is explicitly diagnostic-only and must not implement fixes unless asked.
- Added a compact `Agent Operating Contract` to `AGENTS.md` and mirrored only the always-on subset into `.cursor/rules/execution-discipline.mdc`; this tightened read-before-write, surgical-change, conflict-surfacing, checkpoint, deterministic-logic, and fail-visible expectations without adding new governance files, hooks, skills, CI, dependencies, or runtime code.
- Added secure repo-local skill-routing layer.
- Added `skill-router`, `skill-security-review`, and LifeOS-specific skills.
- Updated `AGENTS.md` and Cursor execution discipline to prefer relevant trusted repo-local skills and review global skills before use.
- Added `docs/CODEX_SKILL_ROUTING.md` plus narrow references from `AGENTS.md`, `skill-router`, and Cursor execution discipline so Codex/plugin routing defaults stay compact, explicit, and token-efficient without broad governance rewrites.
- Hardened Phase 5 parse-capture server boundaries and error safety: parser route logging now emits only sanitized error type metadata (no raw provider/internal message payload), and parser modules include explicit server-runtime guards to block browser execution paths.
- Added route-level regression coverage for `/api/parse-capture` (`apps/web/src/app/api/parse-capture/route.test.ts`) validating parser-status GET behavior, forced mock fallback when AI is unavailable, non-leaky safe error responses, and sanitized error logging on both parser and request-validation failures.
- Added contract-drift guard coverage in `apps/web/src/lib/ai/parseCapture.test.ts` to assert JSON structured-output schema keys stay aligned with `ParseCaptureResponseSchema` keys and prompt-declared `parse_status` literals.
- Added static source-of-truth guard coverage requiring explicit server-runtime checks in parser modules (`apps/web/src/__tests__/sourceOfTruth.test.ts`).
- Added Phase 5D parser regression fixtures for both AI structured-output and mock-fallback coverage: `apps/web/src/lib/ai/fixtures/parseCaptureFixtures.ts` now includes simple task, ambiguous project, multi-area ambiguity, proposed-time-block candidate task, blocker/missing-info, low-confidence, and messy brain-dump fixtures.
- Added offline parser regression tests in `apps/web/src/lib/ai/parseCaptureRegression.test.ts` to assert all fixtures validate against `ParseCaptureResponseSchema`, low-confidence/clarification cases route to triage, invalid enum/invalid draft shape fail validation, and mocked Responses API payloads parse without live OpenAI calls.
- Added schema-level regression coverage in `packages/schemas/src/parse-capture.regression.test.ts` for valid fixture shapes and explicit invalid enum/shape rejection.
- Expanded mock parser fallback coverage in `apps/web/src/lib/ai/parseCaptureService.test.ts` with representative capture inputs to ensure fallback outputs stay schema-valid and triage-safe.
- Added Phase 5C capture parser UI controls and safe status handling: `/capture` now shows parser status (`Mock parser`, `AI parser configured`, `AI parser unavailable`), displays safe non-leaking parser failure copy, supports one-click `Retry with mock parser` recovery after AI parse failures, and explicitly explains when low-confidence parse output is routed to triage.
- Extended `/api/parse-capture` with a safe status contract (`GET`) and hardened failure responses (`POST`) so client UI can surface parser availability/recovery without exposing raw API/internal error details.
- Added regression coverage for parser status rendering, low-confidence triage routing messaging, safe parse-failure UI, and mock-retry recovery flow in `apps/web/src/__tests__/capture.test.tsx`, plus parser status/force-mock service coverage in `apps/web/src/lib/ai/parseCaptureService.test.ts`.
- Fixed `/capture` hydration mismatch caused by session-restored workflow state: `WorkflowProvider` now initializes with deterministic state for SSR/first CSR render, restores persisted session workflow in a post-mount effect, and only writes back to `sessionStorage` after hydration completes.
- Added `WorkflowProvider` regression coverage proving persisted capture items hydrate after first render (instead of being injected into first client render), preventing SSR/CSR markup divergence on `/capture`.
- Added a critical bug-fix branch on top of Phase 4E: persisted execution now ignores terminal/non-active rows when choosing the current Execute target, keeps completion feedback visible after the target becomes inactive, rejects mismatched task/calendar-block execution links, and groups Review area summaries with persisted Supabase area ids.
- Added regression coverage for non-active persisted Execute tasks, persisted Review area rollups, and same-user mismatched execution task/block inputs.
- Restored app route/provider integrity after the merge-conflict cleanup: root layout delegates to the client `apps/web/src/app/components/AppShell.tsx`, workflow routes render under `WorkflowProvider`, and the stale duplicate `apps/web/src/components/AppShell.tsx` was removed.
- Cleaned up AppShell/type source-of-truth drift: added a static guard for the single AppShell import boundary and renamed app-local Phase 2 mock/session view models so canonical entity types remain owned by `@lifeos/schemas`.
- Added route smoke coverage that renders workflow pages through the real app shell/provider instead of only manually wrapping pages in `WorkflowProvider`.
- Hardened `WorkflowProvider` so blocked or unavailable `sessionStorage` falls back to in-memory workflow state instead of crashing the app.
- Hardened `WorkflowProvider` hydration so structurally invalid persisted mock workflow JSON falls back to a fresh initial state instead of crashing primary screens.
- Merged `origin/main` into the inspection branch; resolved conflicts by adopting main’s codebase and dropping unused `getTasksByArea` / `getProposalsByArea` / `getCalendarBlocksByArea` from `mockData.ts` (UI uses workflow context instead).
- Task CRUD (mock path)
- Area model
- Basic calendar draft creation (mock path)
- Added always-on Cursor execution discipline rule for phase gating, plugin restrictions, mock preservation, schema strictness, required validation commands, and final risk/file summary expectations
- Refined plugin policy: strict no-plugin compliance when a prompt forbids plugins; otherwise plugins are allowed when appropriate to task and phase
- Added completion proof requirement for agent work: final handoff must show changed files, tests run, limitations, and docs-updated status
- Phase 2 mock workflow uses dedicated schema exports (`Phase2TimeBlockProposal`, etc.) separate from DB entity schemas in `@lifeos/schemas`
- Added local Supabase scaffold with `supabase/config.toml`, initial V1 schema migration, RLS policies, and seed guidance
- Added a migration contract test that checks required Phase 3 tables, RLS policies, and index coverage
- Added Supabase browser/server client helpers, provider-aware data functions, and `/settings/areas` plus `/capture` pages for Phase 4A (alongside Phase 2 mock workflow)
- Updated shared Zod schemas to validate Phase 4A `areas` and `capture_items` rows at app boundaries
- Added Phase 4A UI tests for Supabase-backed area reads, Supabase-backed capture saves, unauthenticated Supabase error states, and a static guard that browser persistence remains limited to `areas` and `capture_items` without service-role key references.
- Added local Supabase seed data for two Auth users and canonical starter areas, plus a local login page for RLS-backed Phase 4A smoke tests
- Aligned canonical V1 default areas across Phase 2 mock data, Phase 4A provider mock data, Supabase seed data, and tests: Main Job, Personal, Volunteer Work, Side Project.
- Phase 2 mock workflow restores `WorkflowState` from `sessionStorage`; the mock ID counter is resynced from persisted entities (`syncWorkflowIdCounterFromState`) on load and after each state update so generated IDs (`capture-*`, `task-*`, etc.) never reuse numeric suffixes after refresh or reset
- Added opt-in local Supabase RLS tests for Phase 4A `areas` and `capture_items`: user A can access own rows, cannot see user B rows, anon reads are denied, and cross-user capture inserts are blocked.
- Added Phase 4B accepted-draft persistence: triage accepts task drafts into `tasks` and project drafts into `projects`, validates create inputs and returned rows with `@lifeos/schemas`, and leaves rejected/deferred drafts uncommitted.
- Added a narrow Supabase migration granting authenticated Data API access for `areas`, `capture_items`, `projects`, and `tasks`; RLS policies remain unchanged.
- Added Phase 4B tests for task/project acceptance, reject-without-create behavior, mock fallback, source-of-truth boundaries, and opt-in local RLS coverage for `tasks` and `projects`.
- Added Phase 4C local planning persistence: `@lifeos/schemas` now validates proposal create/edit inputs, the data layer lists active tasks/proposals/blocks, creates proposals from persisted task rows, persists edit/reject/accept status changes, and inserts local `calendar_blocks` on acceptance.
- `/calendar` now uses persisted planning rows when Supabase is configured/authenticated and preserves the existing Phase 2 mock planning path when Supabase config is absent.
- Added a narrow Supabase migration granting authenticated Data API access for `time_block_proposals` and `calendar_blocks`; existing RLS policies remain unchanged.
- Added Phase 4C tests for task-to-proposal, proposal edit/reject/accept-to-block, migration grants, UI persistence, mock fallback route smoke, and opt-in local RLS coverage for proposals/blocks.
- Added Phase 4D execution/review persistence: `@lifeos/schemas` validates execution start/mark and review-entry inputs, the data layer persists `execution_sessions` and `review_entries`, `/execute` can start and mark persisted sessions, and `/review` reads persisted tasks/blocks/sessions before creating daily review entries.
- Added a narrow Supabase migration granting authenticated Data API access for `execution_sessions` and `review_entries`; existing RLS policies remain unchanged.
- Added Phase 4D tests for starting/completing/missing execution sessions, related task/block status updates, persisted review entry creation, route smoke coverage, and opt-in local RLS coverage for execution/review rows.
- Added Phase 4E deterministic health dashboard: `/health` now checks mock mode availability, Supabase config, auth/session state, area readability, capture persistence readability, AI parser not-configured status, and Google Calendar not-configured status without AI scoring.
- Added best-effort `health_checks` persistence for authenticated Supabase users, plus a narrow authenticated Data API grant for `health_checks`; existing RLS policies remain unchanged.
- Added helper tests for health status construction/persistence fallback, route smoke coverage for async health loading, static no-OpenAI/service-role guard coverage for the health path, migration grant coverage, and opt-in local RLS coverage for `health_checks`.
- Added Phase 5 parse-capture wiring: `apps/web/src/app/api/parse-capture/route.ts` calls the server-only parser, `store: false` remains enforced, invalid AI output fails safely, missing API key uses mock fallback, and `/capture` persists raw captures before adding parsed task/project drafts to triage.
- Tightened `ParseCaptureResponseSchema` for V1 Structured Outputs: explicit `parse_status`, top-level confidence/triage routing, required nullable fields, task/project drafts only, top-level clarification questions, and a nullable structured ambiguity assessment.
- Added guardrails for the intended Phase 5 staging model: `/capture` tests verify raw capture save happens before parsing and parsed drafts route into local triage state, while source-of-truth tests keep OpenAI parsing behind the server route.
- Added Phase 5B parser control and error hardening: `AI_PARSE_CAPTURE_ENABLED=false` now forces mock fallback even when `OPENAI_API_KEY` exists, Responses API invalid structured output is rejected with explicit schema-validation errors, and source-of-truth guards now check all AI env var names (`AI_MODEL_CHEAP`, `AI_MODEL_STANDARD`, `AI_MODEL_STRONG`) stay out of browser code.
- Added Phase 7B Google Calendar scaffolding only: `google_calendar_connections` stores connection metadata without OAuth token columns, `external_write_events` provides user-scoped audit rows, shared schemas validate both row shapes, `.env.example` documents server-only Google env names without requiring them, and health checks keep Google Calendar as a deterministic optional integration status.
- Added Phase 7C Google Calendar OAuth connection flow: `/settings/areas` now shows Google Calendar connection status plus connect/disconnect controls, `/api/google-calendar/connect` starts server-side OAuth with a short-lived sealed state cookie, `/api/google-calendar/callback` validates state and authenticated user context, and `/api/google-calendar/disconnect` clears the local Google Calendar connection without writing any calendar events.
- Added the token-storage prerequisite phase for Google Calendar: `google_calendar_connections` now stores `encrypted_access_token`, `encrypted_refresh_token`, `token_expires_at`, and `token_type`; `GOOGLE_TOKEN_ENCRYPTION_KEY` is now required for real Google configuration; the callback stores encrypted token material server-side and fails safely if no usable refresh token exists; disconnect clears local encrypted token material without writing calendar events.
- Added Phase 7D advisory Google Calendar free/busy checks for persisted proposals: `/api/google-calendar/freebusy` validates authenticated user and connected calendar state server-side, refreshes the stored Google access token on demand, queries Google free/busy without storing external event details, updates only `conflict_flag` plus minimal `conflict_details_json`, and fails without mutating or deleting the local proposal.
- Updated `/calendar` so persisted proposals show `Conflict not checked`, `No conflict detected`, or `Conflict flagged`, expose a manual `Check conflict` action for persisted proposals only, and keep all proposal acceptance behavior local-only.
- Added regression coverage for the new free/busy route, on-demand token refresh path, proposal conflict badge updates, failure-preserves-proposal behavior, and the new Phase 7D source-of-truth boundary that still forbids client-side Google helpers or any calendar write route.
- Added Phase 7E approval-gated Google Calendar event insert: `/api/google-calendar/create-event` requires authenticated Supabase user context, connected encrypted Google token state, explicit `approved: true`, first-write warning acknowledgement when needed, and a persisted eligible proposal before it calls Google `events.insert`.
- Added server-only event insert and audit helpers: the write path creates a pending `external_write_events` row before the Google call, updates it to `succeeded` or `failed`, maps only minimal proposal/task fields into the Google event body, and stores `google_event_id` only after Google returns an event id.
- Updated `/calendar` with a first-write warning checkbox, an explicit `Create Google Calendar event` action, duplicate-write prevention when a block already has `google_event_id`, and user-visible non-destructive failure handling.
- Added regression coverage for approval-required input, first-write warning requirement, successful event insert storage, failed write audit logging, duplicate insert blocking, unauthenticated/unconnected states, no client-side secret exposure, and no AI-triggered calendar write path.
- Added Phase 7F security hardening: authenticated Data API grants no longer expose encrypted Google token columns or allow direct connection/audit mutation, server routes use a server-only Supabase service-role client for token/audit persistence after validating the user JWT, `calendar_blocks.proposal_id` is unique, Google event inserts use deterministic provider IDs for retry/idempotency, and tests cover the tightened grants, service-role boundary, idempotent insert behavior, and post-Google local persistence failure logging.
- Added Phase 8B shared observability safety scaffolding only: `apps/web/src/lib/observability` now centralizes provider readiness state (`disabled`, `missing_config`, `configured`, `invalid_config`), no-op wrappers, privacy scrubbers, and allowlist-first event-property sanitization without enabling Sentry/PostHog/Langfuse or sending telemetry.
- Extended `/health` deterministic checks with `Observability privacy`, `Sentry`, `PostHog`, and `Langfuse` readiness rows that surface only safe metadata such as provider state, missing/invalid key counts, and transport mode `noop`.
- Added regression coverage for observability sanitization, no-op wrapper behavior, secret-free health output, and a source-of-truth guard that blocks direct vendor SDK imports from `apps/web/src`.
- Completed the pnpm 11 config migration: `pnpm-workspace.yaml` now holds the live `allowBuilds` allowlist plus the repo-wide `protobufjs` override, the dead root `package.json` `pnpm` block is removed, and pnpm no longer warns that those settings are ignored.
- Added Phase 8C Sentry-only monitoring through the shared observability layer: `instrumentation.ts`, `instrumentation-client.ts`, and `sentry.{server,edge}.config.ts` initialize Sentry only when DSNs are present, register runtime hooks for the shared wrapper, and keep `sendDefaultPii`, replay, tracing, and source-map upload disabled.
- Added Sentry-specific observability scrubbing helpers plus regression coverage for client/server config safety, Sentry event sanitization, wrapper-to-runtime routing, and parse-capture route error capture through the shared wrapper.
- Added Phase 8D PostHog-only manual analytics through the shared observability wrapper: root client instrumentation initializes `posthog-js` only when `NEXT_PUBLIC_POSTHOG_TOKEN` and `NEXT_PUBLIC_POSTHOG_HOST` are present, all capture still goes through `@/lib/observability`, event names are restricted to the approved workflow taxonomy, and allowed properties are limited to safe metadata only.
- Added manual analytics points for capture, parse, triage, planning, Google Calendar approval/write outcomes, execution start/complete, review submission, and health view without exporting raw captures, task/project titles, calendar details, AI bodies, email/name, tokens, or secrets.
- Added regression coverage for PostHog config safety, manual-event taxonomy enforcement, allowlist-first property sanitization, disabled autocapture/replay/session-recording settings, and safe no-op behavior when PostHog env vars are absent.
- Added Phase 8E Langfuse-only server-side parse-capture tracing through the shared observability wrapper: `apps/web/langfuse.server.config.ts` initializes Langfuse only on the Node runtime when server-only env vars are present, registers a Langfuse runtime hook, and exports only manual `parse_capture` generations.
- Added metadata-only parse tracing in `parseCaptureWithFallback`: traces now record only safe fields such as parser/provider, model tier, model name, prompt/schema versions, parse status, validation status, latency, fallback flag, token counts when available, and safe error categories.
- Added regression coverage proving Langfuse stays optional/no-op when unconfigured, server-only env names never move into client code, mock fallback remains intact, successful traces stay metadata-only, failures emit only safe categories, validation failures do not leak raw content, and parser regression fixtures still pass unchanged.
- Added Phase 8F hardening and rollout proof: request-error capture now routes through the shared observability wrapper instead of passing raw request headers to a vendor SDK, `/health` now reports active observability providers/transports safely across provider-state combinations, source-of-truth tests now restrict direct vendor SDK imports to approved root bootstrap files, and scrubber coverage now includes nested request-like metadata, headers, cookies, and calendar payloads.
- Added `docs/OBSERVABILITY_RUNBOOK.md` as the Phase 8 rollout/runbook artifact with provider responsibilities, env boundaries, conservative production defaults, enable/disable steps, provider-state matrix, troubleshooting, and the manual smoke plan.

## Known issues

- Historical note: local Supabase verification was previously blocked on this Windows environment because the old local port cluster (`54320-54328`) overlapped the excluded TCP range `54318-54417`. Issue `#76` moves the active local stack to `15431-15439`; keep using `supabase status -o env` for the live API URL and anon key during verification.
- Production verification for issue `#93` is still pending. The code path that previously broke local persisted `/login` and `/settings/areas` is now fixed and locally smoke-tested, but an authenticated Vercel `/login` and `/settings/areas` pass is still required before claiming production proof because deployment protection previously blocked direct verification from this environment.
- Supabase multi-step workflow writes are still issued as separate client requests; future work should move proposal acceptance and execution transitions into transactional server/RPC boundaries if stronger atomicity is required.
- Google Calendar free/busy handling for all-day events is not yet explicitly proven or regression-covered; current checks rely on provider free/busy normalization over the proposal time window and store only minimal checked metadata.
- Additional mobile polish may still be needed on screen-specific content outside Home, but AppShell-level overflow and Home cockpit density/prioritization at 390px are now improved.
- Rescheduling does not yet check all-day events.
- Additional mobile polish may still be needed on screen-specific content outside the Capture/Home daily loop pass, but the core entry path now uses full-width mobile capture actions and explicit route guidance at 390px.
- Historical `ChunkLoadError` findings from Playwright logs mapped to transient dev-only stale chunk 404s (`/_next/static/chunks/...`) during HMR/reload windows; after clean restart and full validation gates, no repository code-path regression was identified in this pass.
- Historical/diagnosed E2E note: route-level `404` / `net::ERR_ABORTED` failures inside the AppShell during Playwright runs were reproduced as cold-route Next dev compilation failures in this Windows + OneDrive workspace, not confirmed runtime route bugs. The current wrapper now prewarms core routes before tests start; see `docs/implementation-notes/2026-06-03-playwright-route-warmup.md`.
- Supabase is scaffolded locally and Phase 4E UI uses it for areas, capture, accepted tasks/projects, local time-block proposals, local calendar blocks, execution sessions, review entries, and health check snapshots.
- Supabase-backed capture and accepted-draft saves require an authenticated Supabase user because RLS policies enforce `auth.uid() = user_id`.
- Persisted planning, execution, review, and health remain local-first. Google Calendar now supports connect/disconnect, manual free/busy checks, and explicit approval-gated event insert from local proposals, but there is still no event update/delete, review/health narrative AI, autonomous scheduling, background job, advanced analytics, full sync, or conflict auto-solver.

## Next recommended tasks

1. Re-run production acceptance smoke from an authenticated deployment session or an explicitly approved temporary bypass path; do not weaken Vercel deployment protection casually just to make issue `#93` look resolved without proof.
2. Start the next Google Calendar implementation phase only after explicit human approval, and keep it split into the two already-planned tracks: all-day conflict-check correctness and app-created event update/cancel.
3. Keep regression hardening active by running `pnpm --filter @lifeos/web test:e2e -- tests/e2e/p0-ux-regression.spec.ts` for every UX-affecting change and updating it when real UX batches intentionally change copy, labels, or keyboard focus paths.
4. Treat `pnpm format:check` as a repo-wide cleanup stream, not a blocker for these scoped UX issues, until the pre-existing formatting drift is resolved intentionally.
5. If navigation-label work is reopened later, update `docs/UX_FLOWS.md` first so runtime labels are not allowed to drift away from the authority doc again.
6. Preserve the current truthfulness boundary: no autonomous rescheduling, no Google update/delete without explicit user action, and no raw provider payload storage.
7. Run the local Supabase end-to-end smoke using `user_a@example.test` / `password123`, then record exact pass/fail/skipped evidence for `/login`, `/settings/areas`, `/capture`, `/triage`, `/calendar`, `/execute`, `/review`, and `/health`.
8. Plan the smallest safe all-day-event conflict-check fix before any calendar implementation work.
9. Plan the exact approved scope for app-created Google Calendar event update/cancel after explicit user approval only.
10. Treat the current UX roadmap as shipped enough for maintenance. Only reopen implementation when a specific route regression, browser-proof failure, or bounded new UX requirement appears.

## Important implementation notes

- Capture feedback is now intentionally single-surface: when organize/parse is in play, do not reintroduce stacked raw-save and parse-success alerts on the same route state unless there is a specific product reason.
- Planning suggestion-oriented updates now intentionally surface `Review next suggested time block` near the success feedback instead of making the user hunt for the next useful move lower in the route.
- Execute pending and failure states now intentionally use the same authored feedback family as its success states; do not drop back to flat muted status text or bare destructive stop points on the primary route path without a specific reason.
- Review save feedback now intentionally sits in the same celebration family as the other primary workflow routes; do not treat review save as a quieter legacy exception unless the route contract changes.
- Health run feedback now intentionally sits next to the control in the same authored family as the other workflow routes; do not regress it back into a flat status line unless the route contract changes.
- Areas create/remove/accent/reset feedback now intentionally lives closer to the relevant control; do not push those outcomes back into scattered footer alerts or muted inline fragments without a specific reason.
- Health now intentionally reads as a trust-and-repair desk, not a generic diagnostics dashboard; keep the trust answer and repair queue dominant over lower-value subsystem detail.
- Areas now intentionally reads as a quiet ownership registry, not a noisy settings grid; keep ownership boundaries and current-area context clearer than admin mechanics.
- Execute now intentionally reads as a mission room, not a generic task panel; keep the one-mission frame stronger than auxiliary state or side-thought surfaces.
- Review now intentionally reads as a carry-forward desk, not a generic summary dashboard; keep closure and carry-forward decisions stronger than broad historical noise.
- `AppShell` now intentionally suppresses the extra shell-context band on `/capture`, `/calendar`, `/execute`, and `/review`. Treat that as the shipped shell contract unless a future pass has a specific reason to reopen it.
- The current production smoke limitation is access-boundary, not app-runtime proof: the public root fetch succeeded on the active Vercel production deployment, but `/login` returned Vercel Authentication from this environment and no temporary bypass URL was created. Treat that as a skipped surface unless a human explicitly approves an authenticated or bypassed smoke path.
- Supabase Data API entity rows may arrive with timezone offsets rather than trailing `Z` datetimes. Persisted browser data helpers now normalize those provider timestamps before schema validation; keep that fix at the provider boundary instead of loosening schemas ad hoc per screen.
- The current all-day calendar issue is a planning/proof gap, not yet a reproduced code-path bug from this run. Today the free/busy helper sends only `timeMin`, `timeMax`, and `items` to Google, stores only `checkedAt`/`hasConflict`, and has no explicit all-day regression coverage. Reproduce before broadening logic.

- PostCSS alert resolution is intentionally policy-level, not a broad package bump. Keep the workspace override in `pnpm-workspace.yaml` until upstream dependency ranges make it unnecessary, and verify the lockfile still resolves a single patched `postcss` version before claiming the alert is closed. The issue-driven source search found app-local PostCSS config use but no direct user-controlled CSS stringify-to-`<style>` embedding path in the shipped app code.
- The daily-flow usability pass is intentionally route-only. Home guidance, Capture ergonomics, and Review log/history must keep using existing links, actions, `review_entries`, and local fallback notes only; do not turn this into hidden navigation logic, sample-data onboarding, a broader analytics dashboard, or new persistence contracts.
- `agent:plan` is intentionally planning-only. `.github/workflows/codex-issue-plan.yml` may read the repo and comment on an issue, but it must not create branches, commits, patches, or implementation PRs. Treat issue text as untrusted input, keep repo guidance authoritative, and use the workflow to recommend the smallest safe route rather than to authorize implementation. `docs/agent/VERIFIED_MEDIUM_AUTOMATION.md` is also policy-only; it does not enable a T2 auto-merge lane by itself.
- Hands-off expansion remains intentionally narrow. `scripts/agent/automation-policy.mjs` is now the shared source for safe auto-merge allowlist rules, low-risk/CI-autofix forbidden paths, and PR-review high-risk path triggers so the control plane does not silently drift across scripts. Keep safe auto-merge constrained to `docs/**`, `README.md`, and `.github/ISSUE_TEMPLATE/**`; keep automation scripts, workflow files, prompts, package manifests, env files, Supabase/auth/parser/calendar/observability surfaces, and similar control-plane/runtime paths out of low-risk and CI-autofix hands-off lanes. `scripts/agent/check-automation-scope.mjs` self-tests are now the quick proof for those shared boundaries and should be updated whenever the policy matrix changes.
- `docs/agent/ROLE_SEPARATED_DELIVERY.md` is engineering workflow guidance only. It does not authorize LifeOS runtime multi-agent behavior or any replacement of the issue -> PR -> CI/review delivery path. Review prompts may compare diffs against issue acceptance criteria and research/spec checkpoints only when that context is available through a trusted channel; otherwise reviewers must say the comparison could not be completed from trusted inputs.
- Safe PR auto-merge must stay revocable, not just enable-only. `.github/workflows/safe-automerge.yml` intentionally listens to `unlabeled` and `converted_to_draft` in addition to the usual PR open/sync events so a PR that loses `automerge:safe`, gains blocking conditions, or goes back to draft has its armed GitHub auto-merge disabled instead of remaining silently eligible. Keep the workflow limited to `contents: read` and `pull-requests: write`, keep changed-file eligibility deterministic in `scripts/agent/check-safe-automerge.mjs`, do not broaden the allowlist beyond docs/README/issue-template paths without explicit review, keep `.github/codex/prompts/**` under human review as control-plane files, keep test-only PRs out of safe auto-merge until a stronger assertion-preservation guard exists, and remember that repo-level GitHub auto-merge must be enabled before the workflow can arm it.
- CI autofix automation must treat branch-controlled repo instructions as untrusted on the failing ref. `.github/workflows/codex-ci-autofix.yml` exports trusted copies of `AGENTS.md`, routing/context docs, `package.json`, `ci.yml`, and the autofix prompt from the default branch into `.git/codex/` before checking out the failing commit, then points `openai/codex-action` at the trusted prompt file. Keep fork/ineligible branches skipped, keep the generate job read-scoped at the GitHub token level, and do not broaden the forbidden-path guard or main-target fallback without an explicit security review.
- `workflow_dispatch` fallback for low-risk issue automation must read `issue_number` from `context.payload.inputs.issue_number` inside `actions/github-script`; `core.getInput('issue_number')` reads action inputs, not workflow inputs, and causes the resolve step to fail before label/state gating runs. Also avoid using the reserved `result` output name on `actions/github-script` steps when downstream conditions depend on a custom parsed outcome, or the built-in return-value output can shadow the intended branch selector.
- Docs-only low-risk issues may infer one existing target doc only when the target is genuinely singular and already owns the referenced repo-level commands or concept. For command clarification, prefer `README.md`; if two plausible docs exist, force human clarification instead of guessing.
- Low-risk issue automation is intentionally bounded twice: the prompt in `.github/codex/prompts/low-risk-implementation.md` constrains Codex to small non-risky changes, and `.github/workflows/codex-low-risk-issue-to-pr.yml` separately blocks forbidden paths (`supabase/migrations/**`, Google Calendar server/client paths, AI contracts, observability, automation prompt files, env files, and dependency manifests) before validation or PR creation. Keep the Codex generation job read-scoped at the GitHub token level, keep write-side validation/PR creation in a later job, and do not add auto-merge.
- PR review automation is now intentionally two-tiered. Use `scripts/agent/classify-pr-risk.mjs` for deterministic escalation logic, keep the baseline reviewer cheap by default (`gpt-5.4-mini` medium), and reserve `gpt-5.4` high for risky labels/paths/size or explicit baseline uncertainty. Keep both review jobs read-only, do not reintroduce patch/push behavior into CI review automation, and keep bot-authored PRs out of the Codex review lane until there is an approved review path that does not rely on permissionless bot actors.
- Durable P0 UX browser-regression coverage lives in `apps/web/tests/e2e/p0-ux-regression.spec.ts` with config at `apps/web/playwright.config.ts`. The suite uses role/label/name-first selectors, runs against local/dev mode, and does not perform Google Calendar writes.
- `pnpm --filter @lifeos/web test:e2e` is now the safer Windows-friendly entry point because it owns dev-server cleanup itself; direct `playwright test` still relies on Playwright’s config-managed `webServer` path.
- `apps/web/components.json` and `apps/web/postcss.config.mjs` now initialize app-local shadcn-style component wiring and Tailwind v4 postcss integration. Keep UI work token-first through `globals.css` variables instead of ad-hoc per-page color literals.
- Frontend consistency direction is now explicit repo policy: push shared primitives and repeated interaction patterns toward app-local shadcn-compatible components, but keep shell identity, route-level authorship, area accents, and flagship/support/admin composition intentionally custom instead of forcing a full stock-shadcn rewrite.
- Shared frontend primitives now include app-local `Label` and `Skeleton`, and repeated system-details disclosures on touched routes should go through `DiagnosticsDisclosure` instead of reintroducing hand-written `details/summary` markup unless the route needs a clearly different treatment.
- The current repeated-seam cleanup is complete at the route level: outside the shared abstraction layer itself, primary workflow and settings routes should not reintroduce raw `details/summary` wrappers or unlabeled primitive drift when `DiagnosticsDisclosure`, `Label`, `Skeleton`, or `WorkflowLoadingState` already fit.
- The active UX roadmap and the older supporting UX plans are now aligned on that same frontend split. Future UI planning should treat shadcn-compatible primitives as the shared foundation and route-level composition as intentionally custom, rather than reopening primitive cleanup as a standalone modernization objective.
- `AppShell` now intentionally suppresses the extra shell-context band on `/execute` and `/review`. Area context still stays visible there through the shell status control and route-local cards, but the extra shell band is gone on purpose so route-local action wins the first scan.
- `apps/web/src/components/theme-provider.tsx` includes a safe `matchMedia` fallback for test environments that do not provide it; avoid removing this unless tests are updated to polyfill consistently.
- Future agents should use `docs/agent/CONTEXT_INDEX.md` or `pnpm agent:context <area>` before broad repo exploration.
- The agent context helper and `docs/agent/REPO_MAP.json` are orientation aids, not authority sources; keep `REPO_MAP.json` concise and update it only when paths or risk surfaces materially change.
- `docs/skills/next-phase-gate-review.md` is the canonical phase-boundary diagnostic review for this repo. Use it to assess fragility, duplication, production risk, missing tests/proof, and scope control, but do not let it turn into an implementation pass or a broad refactor plan.
- Phase 8B observability is intentionally scaffolding only. The wrapper layer must remain vendor-neutral and no-op until a later phase explicitly enables a specific adapter. Do not add SDK init, network export, replay, autocapture, prompt/completion tracing, or raw-content logging as “small follow-ups” inside this phase.
- Phase 8C is Sentry only. Keep all app-level error capture routed through `@/lib/observability`, keep vendor imports out of `apps/web/src` except for root-level Sentry bootstrap files, and do not enable Replay, `sendDefaultPii`, tracing, source-map upload, or any prompt/completion export.
- Phase 8D is PostHog manual analytics only. Keep vendor imports out of `apps/web/src`, do not enable autocapture, pageview/navigation capture, session recording, heatmaps, dead clicks, console-log capture, or identify calls, and restrict event payloads to the small approved metadata allowlist.
- Phase 8E is Langfuse server-only parse-capture tracing only. Keep vendor imports out of `apps/web/src`, initialize Langfuse only from root-level server bootstrap files, use manual observations instead of prompt/completion-capturing wrappers, and export metadata only for `parse_capture`.
- Phase 8F keeps observability hardening narrow. Do not add new vendors, replay, autocapture, broad tracing, prompt/completion export, or raw-content logging; use `docs/OBSERVABILITY_RUNBOOK.md` for rollout verification and provider-state smoke checks.
- The new `Agent Operating Contract` is governance-only. It reinforces existing LifeOS invariants and failure handling, but it does not change product requirements, architecture, runtime behavior, schema boundaries, validation standards, or approval-gated Google Calendar rules.
- `.github/AGENT_AUTOMATION_POLICY.md` is governance-only. It adds explicit human-gate tiering for agent-driven repo work; it does not relax any existing validation, schema, RLS, auth, privacy, or external-write constraints.
- Future agents should run `.agents/skills/skill-router/SKILL.md` before substantial work.
- Repo-local LifeOS skills are preferred over global skills.
- Global/user-level skills should be treated as lower-trust and reviewed with `skill-security-review` before being followed.
- Skills cannot override `AGENTS.md`, user instructions, security/privacy rules, schema/RLS rules, external-write approval gates, or validation requirements.
- `WorkflowProvider` must keep SSR and first client render structurally identical. Session persistence restore belongs in a client effect boundary, not reducer initialization, so `/capture` and other workflow routes do not hydrate with mismatched markup when mock/session data exists.
- Domain types in `@lifeos/types` are re-exports of Zod-inferred types from `@lifeos/schemas`; `packages/types/src/schema-type-parity.ts` is a compile-time check that `Area`, `Capture`/`CaptureItem`, and other re-exports stay aligned (fails `tsc` if `index.ts` is replaced with divergent manual interfaces).
- App-local mock/session-only types use `Phase2Mock...` names in `apps/web/src/lib/types.ts`; do not reintroduce canonical names like `Task`, `Project`, `CalendarBlock`, `ExecutionSession`, or `HealthCheck` there.
- Task status and TimeBlock status are separate.
- Calendar events are never auto-deleted without confirmation.
- Agent guidance is now aligned across `AGENTS.md` and `.cursor/rules/execution-discipline.mdc` for phase-first implementation and completion checks.
- Phase 3 migration covers only the requested V1 tables: areas, capture items, projects, tasks, proposals, calendar blocks, execution sessions, review entries, health checks/incidents, suggestion records, and override records.
- RLS policies use `to authenticated` and `((select auth.uid()) = user_id)`; area/project/task references use same-user composite foreign keys to reduce cross-user contamination risk.
- Phase 4D data access falls back to mock data when `NEXT_PUBLIC_SUPABASE_URL` or `NEXT_PUBLIC_SUPABASE_ANON_KEY` is missing.
- Phase 5 adds OpenAI only behind the server `/api/parse-capture` route. Browser components must not import `apps/web/src/lib/ai/parseCapture.ts`; they should call the route and use `parseCaptureWorkflow.ts` for client-safe conversion into triage state.
- AI parse-capture output is not committed as tasks/projects directly. It becomes reviewable local task/project drafts; accepted triage drafts still pass through the existing `createTask` / `createProject` Zod validation and Supabase RLS paths.
- Do not add durable draft persistence tables unless requirements explicitly change. Current intended flow is durable raw capture -> local parsed drafts -> durable accepted task/project.
- Local Supabase seed users both use password `password123`; User A has Main Job, Personal, Volunteer Work, and Side Project areas, while User B has a private area for RLS isolation checks.
- `supabase db reset` has been verified locally after the seed update.
- `apps/web/src/__tests__/phase4aRls.local.test.ts` is skipped by default; run it with `RUN_SUPABASE_RLS_TESTS=1`, local Supabase URL, and the local anon key from `supabase status -o env`. It now covers `areas`, `capture_items`, `tasks`, `projects`, `time_block_proposals`, `calendar_blocks`, `execution_sessions`, `review_entries`, `health_checks`, `google_calendar_connections`, and `external_write_events`.
- Google Calendar configuration now requires `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`, and `GOOGLE_TOKEN_ENCRYPTION_KEY`. If the token key is absent, the settings UI must stay mock-safe and report Google Calendar as unavailable instead of attempting OAuth.
- Real Google token/audit persistence also requires server-only `SUPABASE_SERVICE_ROLE_KEY`. Do not put it in `NEXT_PUBLIC_`, do not import the service-role helper from browser/client code, and do not use it to bypass user ownership checks; routes must validate the Supabase user token first and then filter service-role writes by that user id.
- There is no single production toggle or `DEMO_MODE=false` path. Production rollout means setting the full Vercel env contract for Supabase, optional AI parsing, and optional Google Calendar features; otherwise the app will truthfully degrade to Demo mode on the affected surfaces.
- `google_calendar_connections` now stores encrypted server-only OAuth token material. Authenticated browser Data API grants must not expose encrypted token columns or allow direct connection mutation. Client routes and UI must continue to select only the public-safe columns, and `external_write_events` must still store summaries only, never raw Google payloads or secrets.
- The OAuth callback still uses a short-lived sealed HttpOnly cookie carrying the initiating Supabase access token only long enough to finish the RLS-authenticated Google Calendar connection write, then clears it immediately.
- Phase 7D free/busy checks remain advisory only. They must not auto-reschedule, must not block local proposal persistence, and must not store raw Google event payloads or any more detail than minimal checked status metadata.
- Phase 7E event creation is server-only and approval-gated. Browser code may call the local `create-event` route after a user click, but must never import Google Calendar event helpers, token helpers, OAuth helpers, or AI modules.
- Failed Google event inserts must leave the local proposal and blocks unchanged and must update the pending `external_write_events` row to `failed`. Duplicate event creation is blocked by checking existing local blocks for `google_event_id` before calling Google, by a unique `calendar_blocks.proposal_id` constraint, and by deterministic Google event IDs (`lifeos` + proposal UUID without dashes) so retrying the same approved proposal does not create a second external event.
- `WorkflowProvider` should remain usable when browser storage is unavailable or contains invalid mock workflow state; persistence failures are intentionally swallowed after ID-counter sync.
- The new Triage queue is intentionally deterministic and narrow: task drafts stay ahead of project drafts, only one item is expanded at a time, and tests that need a non-current item must switch it into focus with `Review this next` instead of assuming every draft stays expanded.
- Triage maps Phase 2 mock area ids to persisted Phase 4 area slugs before saving accepted tasks/projects; `/calendar` creates persisted proposals from persisted task ids rather than from Phase 2 mock proposal draft ids.
- `/execute` should only auto-select active persisted tasks or non-terminal persisted execution sessions; completed/skipped/blocked rows must not become the next runnable target.
- `/review` must use persisted area ids/names when summarizing Supabase-backed tasks and execution sessions; Phase 2 mock area ids only apply to mock workflow state.
