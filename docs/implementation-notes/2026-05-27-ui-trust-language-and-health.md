# UI trust-language and health follow-up

- Task name and branch: issues `#80`, `#81`, `#82`, `#83` on `feat/80-83-ui-trust-language`
- Original scope: replace leaked implementation jargon in normal workflow UI, unify diagnostics disclosure copy, clarify AppShell area/save trust messaging, and make `/health` answer daily trustworthiness first without changing persistence/auth/calendar/parser logic
- Assumptions:
  - status-language changes are acceptable as long as behavior and safety boundaries stay unchanged
  - diagnostics can stay accessible through collapsed disclosures instead of separate `System details` / `Developer details` sections
- Decisions:
  - added shared `statusVocabulary` helpers so save/sync, AI sorting, and calendar wording stay consistent
  - added one shared `DiagnosticsDisclosure` component instead of repeating ad hoc `<details>` blocks
  - kept `/health` deterministic and built the new trust summary strictly from existing health checks
  - kept optional telemetry checks in diagnostics only so they remain visible but secondary
- Deviations:
  - widened the copy sweep slightly to include `/login` and Google Calendar settings messaging because `mock mode` leakage was still user-visible there
- Tradeoffs:
  - source-of-truth guard coverage now partly reads the shared vocabulary helper because some plain-language strings no longer live inline in page files
  - `/health` now uses more derived presentation logic in the page component, but the underlying check model is unchanged
- Files changed and why:
  - `apps/web/src/lib/statusVocabulary.ts`: shared save/sync/AI/calendar wording
  - `apps/web/src/app/components/DiagnosticsDisclosure.tsx`: shared collapsed diagnostics pattern
  - `apps/web/src/app/components/AppShell.tsx`: plain-language area/save trust cleanup
  - `apps/web/src/app/{page,capture,triage,calendar,execute,review,health,login}/...`: applied shared vocabulary and diagnostics pattern, rewrote `/health` top summary
  - `apps/web/src/app/settings/areas/{page,GoogleCalendarConnectionPanel.tsx}`: admin/connection wording cleanup
  - `apps/web/src/__tests__/*`: updated smoke, source-of-truth, health, capture, login, page, and persistence regressions to match truthful copy
- Validation commands and results:
  - `pnpm --filter @lifeos/web test -- routeSmoke.test.tsx sourceOfTruth.test.ts healthPage.test.tsx capture.test.tsx page.test.tsx login.test.tsx phase4aPersistence.test.tsx` ✅
  - `pnpm --filter @lifeos/web type-check` ✅
  - `pnpm --filter @lifeos/web lint` ✅
  - `pnpm --filter @lifeos/web test` ✅
  - `pnpm --filter @lifeos/web build` ✅
- Risks:
  - copy-sensitive browser/E2E flows outside the current regression net could still need minor assertion updates
  - `/health` trust-summary heuristics are presentation-only, but future health-check additions should decide whether they belong in top summary or diagnostics-only
- Deferred items:
  - screen-specific workflow simplification issues (`#84` onward) remain separate
  - a true developer-mode surface is still separate from this diagnostics cleanup
- Rollback notes:
  - revert this branch’s UI/helper/test commits only; no data model, auth, parser, calendar-write, or env behavior changed
