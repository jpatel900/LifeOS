# docs/\_archive — frozen history

Everything here is HISTORICAL: superseded docs kept for provenance. Each file's header says when it was archived and what replaced it. Agents: skip this folder when looking for current truth — start at `docs/PROJECT_STATE.md`. Files here are never updated (fix the successor doc instead) and are excluded from formatting.
