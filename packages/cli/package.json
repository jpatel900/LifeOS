{
  "name": "@lifeos/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Headless LifeOS client for agents, scripts, and CI (ADR 0006). Thin: all data operations go through the versioned /api/v1 server contracts; Supabase is used for auth only.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "lifeos": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^4"
  }
}
