{
  "name": "@lifeos/types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "test": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@lifeos/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
